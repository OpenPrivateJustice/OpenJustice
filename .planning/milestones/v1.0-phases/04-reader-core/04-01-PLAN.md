---
phase: 04-reader-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - OpenJustice.sln
  - src/OpenJustice.Reader/OpenJustice.Reader.csproj
  - src/OpenJustice.Reader/Program.cs
  - src/OpenJustice.Reader/wwwroot/appsettings.json
  - src/OpenJustice.Reader/Configuration/ReaderOptions.cs
  - src/OpenJustice.Reader/Services/AppState/ReaderAppState.cs
  - src/OpenJustice.Reader/Components/Layout/NavMenu.razor
  - src/OpenJustice.Reader/Pages/Home.razor
autonomous: true
requirements:
  - RDR-01
  - RDR-05
must_haves:
  truths:
    - "Reader opens as a client-side Blazor WebAssembly SPA"
    - "Reader reads required runtime settings from appsettings.json"
    - "Main navigation exposes reader entry points (sync, search, cases)"
  artifacts:
    - path: "src/OpenJustice.Reader/OpenJustice.Reader.csproj"
      provides: "Standalone Reader WASM project"
    - path: "src/OpenJustice.Reader/wwwroot/appsettings.json"
      provides: "Reader runtime configuration for torrent/local DB"
    - path: "src/OpenJustice.Reader/Configuration/ReaderOptions.cs"
      provides: "Strongly typed options and validation"
  key_links:
    - from: "src/OpenJustice.Reader/Program.cs"
      to: "src/OpenJustice.Reader/Configuration/ReaderOptions.cs"
      via: "DI options binding"
      pattern: "AddOptions<ReaderOptions>|Bind\("
    - from: "src/OpenJustice.Reader/Components/Layout/NavMenu.razor"
      to: "src/OpenJustice.Reader/Pages/*.razor"
      via: "route links"
      pattern: "NavLink"
---

<objective>
Bootstrap the public Reader application as an independent Blazor WebAssembly SPA with validated runtime configuration.

Purpose: Establish the base app shell and config system all reader features depend on.
Output: Reader project, appsettings, options binding, and baseline navigation.
</objective>

<execution_context>
@/home/eduardo/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/eduardo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-database-configuration-foundation/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Reader WASM project and wire solution</name>
  <files>OpenJustice.sln, src/OpenJustice.Reader/OpenJustice.Reader.csproj, src/OpenJustice.Reader/Program.cs, src/OpenJustice.Reader/Components/Layout/NavMenu.razor, src/OpenJustice.Reader/Pages/Home.razor</files>
  <action>Create a new .NET 10 Blazor WebAssembly project at src/OpenJustice.Reader and add it to OpenJustice.sln. Keep project isolated from Generator.Web runtime (public reader must run as standalone SPA). Register core services in Program.cs and add navigation entries for Sync, Search, and Cases pages even if placeholder pages are minimal initially.</action>
  <verify>dotnet sln OpenJustice.sln list && dotnet build src/OpenJustice.Reader/OpenJustice.Reader.csproj</verify>
  <done>Reader project builds successfully and appears in solution; app launches with SPA shell and navigation links.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Reader configuration model and startup validation</name>
  <files>src/OpenJustice.Reader/wwwroot/appsettings.json, src/OpenJustice.Reader/Configuration/ReaderOptions.cs, src/OpenJustice.Reader/Program.cs</files>
  <action>Add reader-specific appsettings.json containing torrent trackers, snapshot/version endpoint metadata, and local database file settings. Create ReaderOptions class with DataAnnotations and explicit section mapping. In Program.cs bind options and fail startup when required values are missing; mirror existing options pattern from Generator (IOptions + validation) instead of ad-hoc config reads.</action>
  <verify>dotnet build src/OpenJustice.Reader/OpenJustice.Reader.csproj</verify>
  <done>Reader config is strongly typed, validated, and consumed via DI; no feature code reads raw IConfiguration directly.</done>
</task>

</tasks>

<verification>
Run the Reader project and confirm it starts as a WASM SPA with no runtime config binding exceptions.
</verification>

<success_criteria>
RDR-01 and RDR-05 are satisfied by a runnable SPA shell with validated appsettings-backed configuration.
</success_criteria>

<output>
After completion, create `.planning/phases/04-reader-core/04-01-SUMMARY.md`
</output>
