---
phase: 10-wire-reader-to-generator-history-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/OpenJustice.Reader/Configuration/ReaderOptions.cs
  - src/OpenJustice.Reader/wwwroot/appsettings.json
  - src/OpenJustice.Reader/Services/Cases/IGeneratorHistoryApiClient.cs
  - src/OpenJustice.Reader/Services/Cases/GeneratorHistoryApiClient.cs
  - src/OpenJustice.Reader/Program.cs
autonomous: true
requirements:
  - RDR-22
must_haves:
  truths:
    - "Reader sends authenticated requests to Generator history endpoints for both full timeline and field-specific timeline."
    - "If Generator responds 401, Reader executes an explicit auth recovery path (redirect to configured login URL)."
    - "History endpoint JSON is mapped into Reader history view models without losing ChangedAt, field values, or ChangeConfidence."
  artifacts:
    - path: "src/OpenJustice.Reader/Configuration/ReaderOptions.cs"
      provides: "Generator history API + auth settings with startup validation."
      contains: "class GeneratorHistoryApiOptions"
    - path: "src/OpenJustice.Reader/Services/Cases/IGeneratorHistoryApiClient.cs"
      provides: "Contract for authenticated history API calls and unauthorized signaling."
      contains: "GetCaseHistoryAsync"
    - path: "src/OpenJustice.Reader/Services/Cases/GeneratorHistoryApiClient.cs"
      provides: "HTTP implementation with Authorization header and 401 handling."
      contains: "HttpStatusCode.Unauthorized"
    - path: "src/OpenJustice.Reader/Program.cs"
      provides: "DI wiring for GeneratorHistoryApiClient using configured base URL/options."
      contains: "AddSingleton<IGeneratorHistoryApiClient"
  key_links:
    - from: "src/OpenJustice.Reader/Program.cs"
      to: "src/OpenJustice.Reader/Configuration/ReaderOptions.cs"
      via: "DI registration reads GeneratorHistoryApi options"
      pattern: "GeneratorHistoryApi"
    - from: "src/OpenJustice.Reader/Services/Cases/GeneratorHistoryApiClient.cs"
      to: "Generator /api/cases/{id}/history endpoints"
      via: "HttpClient.SendAsync with Authorization header"
      pattern: "Authorization"
    - from: "src/OpenJustice.Reader/Services/Cases/GeneratorHistoryApiClient.cs"
      to: "Configured login URL"
      via: "401 branch redirects with NavigationManager"
      pattern: "Unauthorized"
---

<objective>
Create the Reader-side API integration foundation so history data can be fetched live from Generator with authenticated requests and deterministic unauthorized handling.

Purpose: Replace local-only history plumbing with a network-ready, auth-aware client before service/UI refactor.
Output: Reader options + authenticated GeneratorHistoryApiClient + DI wiring.
</objective>

<execution_context>
@/home/eduardo/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/eduardo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/OpenJustice.Reader/Configuration/ReaderOptions.cs
@src/OpenJustice.Reader/Program.cs
@src/OpenJustice.Generator/Controllers/CaseHistoryController.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Generator history API/auth configuration with fail-fast validation</name>
  <files>src/OpenJustice.Reader/Configuration/ReaderOptions.cs, src/OpenJustice.Reader/wwwroot/appsettings.json</files>
  <action>Add a new GeneratorHistoryApi options section in ReaderOptions containing at minimum BaseUrl, AccessToken, and LoginUrl (plus optional request timeout). Bind and validate it with the existing fail-fast validator path so startup fails when required auth/API fields are missing. Keep existing Torrent/Snapshot/LocalDb validation unchanged. In appsettings.json, add placeholder values (no real secret) showing the required keys and expected shape.</action>
  <verify>dotnet build src/OpenJustice.Reader/OpenJustice.Reader.csproj</verify>
  <done>Reader configuration includes a validated GeneratorHistoryApi section and documents required auth/API keys in appsettings.json.</done>
</task>

<task type="auto">
  <name>Task 2: Implement authenticated Generator history API client and register it in DI</name>
  <files>src/OpenJustice.Reader/Services/Cases/IGeneratorHistoryApiClient.cs, src/OpenJustice.Reader/Services/Cases/GeneratorHistoryApiClient.cs, src/OpenJustice.Reader/Program.cs</files>
  <action>Create a dedicated client abstraction for GET /api/cases/{caseId}/history and GET /api/cases/{caseId}/history/{fieldName}. Every request must attach Authorization: Bearer {AccessToken} from options (do not call endpoints without auth header). Handle 404 as empty result, and handle 401 explicitly by signaling unauthorized and redirecting to configured LoginUrl (token refresh is not available in current codebase). Deserialize JSON into Reader history view-model shape preserving ChangeConfidence and timestamps. Register the client in Program.cs with base address from GeneratorHistoryApi.BaseUrl (not HostEnvironment.BaseAddress).</action>
  <verify>dotnet build src/OpenJustice.Reader/OpenJustice.Reader.csproj && grep -n "Authorization\|Unauthorized\|LoginUrl" src/OpenJustice.Reader/Services/Cases/GeneratorHistoryApiClient.cs</verify>
  <done>Generator history endpoints are callable through a dedicated client that always sends auth headers and has an explicit 401 redirect path.</done>
</task>

</tasks>

<verification>
1. Build Reader project successfully.
2. Confirm new options validation includes GeneratorHistoryApi required fields.
3. Confirm GeneratorHistoryApiClient contains Authorization header assignment and explicit 401 redirect handling.
</verification>

<success_criteria>
1. Reader has a dedicated history API client targeting Generator endpoints.
2. All history API requests include Authorization header.
3. 401 is handled explicitly by redirecting to configured login URL.
</success_criteria>

<output>
After completion, create `.planning/phases/10-wire-reader-to-generator-history-api/10-01-SUMMARY.md`
</output>
