---
phase: 08-wire-torrent-import-pipeline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/OpenJustice.Reader/Services/Sync/ISyncServices.cs
  - src/OpenJustice.Reader/Services/Sync/TorrentSyncService.cs
  - src/OpenJustice.Reader/Services/Data/SqliteCaseStore.cs
  - src/OpenJustice.Reader/Pages/Sync/Sync.razor
autonomous: true
requirements:
  - RDR-02
  - RDR-03
  - RDR-04
must_haves:
  truths:
    - "When sync runs, downloaded snapshot SQL is actually imported into the local Reader data store."
    - "Reader SQL parser accepts Generator table naming for \"Cases\" and \"CaseFieldHistory\" statements."
    - "Production sync flow does not rely on hardcoded demo SQL blobs."
    - "After import, search/filter pages use imported snapshot data and return complete results."
  artifacts:
    - path: "src/OpenJustice.Reader/Services/Sync/TorrentSyncService.cs"
      provides: "download -> import orchestration in SyncAsync"
      contains: "ImportSnapshotAsync"
    - path: "src/OpenJustice.Reader/Services/Data/SqliteCaseStore.cs"
      provides: "Parser compatibility with Generator SQL naming contract"
      contains: "Cases"
    - path: "src/OpenJustice.Reader/Pages/Sync/Sync.razor"
      provides: "UI sync actions that call real import flow without demo SQL"
      contains: "LoadDatabase"
  key_links:
    - from: "TorrentSyncService.SyncAsync"
      to: "SqliteCaseStore.ImportSnapshotAsync"
      via: "downloaded SQL payload"
      pattern: "ImportSnapshotAsync"
    - from: "SqliteCaseStore regex parser"
      to: "Generator snapshot SQL"
      via: "INSERT INTO table-name matching"
      pattern: "Cases|CaseFieldHistory"
    - from: "Sync.razor actions"
      to: "ITorrentSyncService"
      via: "DownloadLatest/ReloadDatabase"
      pattern: "SyncAsync"
---

<objective>
Wire Reader sync end-to-end so torrent/HTTP snapshot download flows directly into SQL import with no demo fallback, and align parser with Generator naming contract.

Purpose: Close the integration gap that currently breaks real data ingestion and downstream search accuracy.
Output: Real sync-to-import pipeline, parser contract fixes, and production-safe sync page behavior.
</objective>

<execution_context>
@/home/eduardo/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/eduardo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@src/OpenJustice.Reader/Services/Sync/ISyncServices.cs
@src/OpenJustice.Reader/Services/Sync/TorrentSyncService.cs
@src/OpenJustice.Reader/Services/Data/SqliteCaseStore.cs
@src/OpenJustice.Reader/Pages/Sync/Sync.razor
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire SyncAsync to import downloaded SQL into local store</name>
  <files>src/OpenJustice.Reader/Services/Sync/ISyncServices.cs, src/OpenJustice.Reader/Services/Sync/TorrentSyncService.cs</files>
  <action>Refactor sync contracts so download result carries SQL payload (or equivalent import-ready content) and SyncAsync calls CaseStore.ImportSnapshotAsync as part of the normal success path. Return import success/failure in sync result, propagate actionable error messages, and only persist local version when import succeeds. Avoid placeholder file-path-only success that does not mutate local data.</action>
  <verify>dotnet build src/OpenJustice.Reader/OpenJustice.Reader.csproj</verify>
  <done>SyncAsync performs real download→import orchestration and reports failure when import fails.</done>
</task>

<task type="auto">
  <name>Task 2: Align SQL parser with Generator table contract and remove demo SQL path</name>
  <files>src/OpenJustice.Reader/Services/Data/SqliteCaseStore.cs, src/OpenJustice.Reader/Pages/Sync/Sync.razor</files>
  <action>Update SQL parsing patterns to accept Generator snapshot table naming forms for Cases/CaseFieldHistory (including quoted and/or schema-qualified INSERT variants) while preserving existing behavior. Remove hardcoded demo SQL from Sync page and replace LoadDatabase/reload behavior with real imported snapshot flow (or explicit unsupported message if no downloaded payload exists). Production code must not ship sample SQL data.</action>
  <verify>grep -n "demoSql\|INSERT INTO cases (id, reference_code" src/OpenJustice.Reader/Pages/Sync/Sync.razor && echo "FAIL if match" || echo "PASS: no demo SQL"</verify>
  <done>Reader imports actual snapshot SQL contract and Sync page contains no embedded demo INSERT dataset.</done>
</task>

<task type="auto">
  <name>Task 3: Verify imported snapshot is searchable and complete</name>
  <files>src/OpenJustice.Reader/Pages/Sync/Sync.razor</files>
  <action>Add or update post-sync status/logging to show imported record counts and make completeness visible (cases + history counts where available). Ensure sync UI flow reflects imported data readiness so Search page uses imported dataset, not placeholders.</action>
  <verify>dotnet build src/OpenJustice.Reader/OpenJustice.Reader.csproj</verify>
  <done>After successful sync/import, stats reflect real imported data and search features operate over that dataset.</done>
</task>

</tasks>

<verification>
Build Reader project and confirm: sync imports real SQL, parser accepts Generator naming contract, and no production demo SQL remains.
</verification>

<success_criteria>
- RDR-02: Local Reader data store is populated by actual downloaded snapshot SQL.
- RDR-03: Sync flow delivers and imports snapshot updates end-to-end.
- RDR-04: Imported database powers search/filter flows with complete data.
- Phase 8 criterion met: no hardcoded demo SQL remains in production path.
</success_criteria>

<output>
After completion, create `.planning/phases/08-wire-torrent-import-pipeline/08-02-SUMMARY.md`
</output>
