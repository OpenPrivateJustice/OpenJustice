---
phase: 02-generator-core
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/Atrocidades.Generator.Api/Controllers/CurationController.cs
  - src/Atrocidades.Generator.Api/Contracts/Curation/ApproveCaseRequest.cs
  - src/Atrocidades.Generator.Api/Domain/Enums/CurationStatus.cs
  - src/Atrocidades.Generator.Api/Services/Curation/CurationService.cs
  - src/Atrocidades.Generator.Api/Services/History/CaseAuditLogService.cs
  - tests/Atrocidades.Generator.Api.Tests/Curation/CurationServiceTests.cs
autonomous: true
requirements: [GEN-06, GEN-07, GEN-08]
must_haves:
  truths:
    - "Curator can approve or reject a case through API workflow endpoints."
    - "Curator can mark a case as verified and the curator identity is stored."
    - "Status and verification actions are recorded in immutable audit history entries."
  artifacts:
    - path: "src/Atrocidades.Generator.Api/Controllers/CurationController.cs"
      provides: "Approval/rejection/verification endpoints"
      exports: ["POST /api/curation/cases/{id}/approve", "POST /api/curation/cases/{id}/reject", "POST /api/curation/cases/{id}/verify"]
    - path: "src/Atrocidades.Generator.Api/Services/Curation/CurationService.cs"
      provides: "State transition rules and curator attribution"
      contains: "Approve|Reject|Verify"
    - path: "src/Atrocidades.Generator.Api/Services/History/CaseAuditLogService.cs"
      provides: "Append-only case action logs"
      contains: "Add"
  key_links:
    - from: "CurationController"
      to: "CurationService"
      via: "workflow endpoint handlers"
      pattern: "ApproveAsync|RejectAsync|VerifyAsync"
    - from: "CurationService"
      to: "CaseAuditLogService"
      via: "append audit entry per transition"
      pattern: "Audit|History"
---

<objective>
Implement curation workflow controls (approve/reject/verify) and immutable audit logging for generator case governance.

Purpose: Ensure only curated records become official and every decision is traceable.
Output: Curation API endpoints, transition service, verification marker handling, and audit log integration tests.
</objective>

<execution_context>
@/home/eduardo/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/eduardo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-generator-core/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build approval/rejection/verification API workflow</name>
  <files>
    src/Atrocidades.Generator.Api/Controllers/CurationController.cs
    src/Atrocidades.Generator.Api/Contracts/Curation/ApproveCaseRequest.cs
    src/Atrocidades.Generator.Api/Domain/Enums/CurationStatus.cs
    src/Atrocidades.Generator.Api/Services/Curation/CurationService.cs
  </files>
  <action>Create curation endpoints that transition cases through pending/approved/rejected/verified states with explicit guard rules. Persist curator identity for approval and verification actions. Prevent invalid transitions (e.g., verify before approval) and return 409 for state-conflict operations.</action>
  <verify>dotnet test tests/Atrocidades.Generator.Api.Tests --filter "FullyQualifiedName~CurationServiceTests"</verify>
  <done>Cases can be approved/rejected/verified only through valid transitions and every action records curator identity fields.</done>
</task>

<task type="auto">
  <name>Task 2: Wire append-only audit logging for curation actions</name>
  <files>
    src/Atrocidades.Generator.Api/Services/History/CaseAuditLogService.cs
    src/Atrocidades.Generator.Api/Services/Curation/CurationService.cs
    tests/Atrocidades.Generator.Api.Tests/Curation/CurationServiceTests.cs
  </files>
  <action>Implement append-only audit entries for each curation action (approve/reject/verify), including case id, action type, previous status, new status, curator id, and timestamp. Do not update prior log rows; always append. Ensure service writes logs atomically with state transitions.</action>
  <verify>dotnet test tests/Atrocidades.Generator.Api.Tests --filter "FullyQualifiedName~CurationServiceTests"</verify>
  <done>Every successful curation action writes a new immutable audit record tied to the case and curator.</done>
</task>

</tasks>

<verification>
Run: `dotnet test tests/Atrocidades.Generator.Api.Tests` and verify transition constraints, curator attribution, and append-only audit behavior are covered and passing.
</verification>

<success_criteria>
- Approval workflow enforces curated publication path.
- Verified marker is set with curator identity retained.
- Action history is append-only and queryable for audit.
</success_criteria>

<output>
After completion, create `.planning/phases/02-generator-core/02-02-SUMMARY.md`
</output>
