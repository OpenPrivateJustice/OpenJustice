---
phase: 02-generator-core
plan: 05
type: execute
wave: 4
depends_on: ["02-02", "02-04"]
files_modified:
  - src/Atrocidades.Generator.Api/Controllers/CasesEvidenceController.cs
  - src/Atrocidades.Generator.Api/Controllers/CasesTagsController.cs
  - src/Atrocidades.Generator.Api/Services/Export/SnapshotExportService.cs
  - src/Atrocidades.Generator.Api/Services/Export/SnapshotVersionService.cs
  - src/Atrocidades.Generator.Api/Options/GeneratorOptions.cs
  - src/Atrocidades.Generator.Api/Program.cs
  - tests/Atrocidades.Generator.Api.Tests/Export/SnapshotExportServiceTests.cs
  - tests/Atrocidades.Generator.Api.Tests/Cases/CasesMetadataTests.cs
autonomous: true
requirements: [GEN-12, GEN-13, GEN-15, GEN-16, GEN-17]
must_haves:
  truths:
    - "Curator can associate evidence and tags with cases through API endpoints."
    - "Generator exports complete PostgreSQL snapshot SQL files."
    - "Snapshot files are versioned sequentially (v1, v2, ...) and configurable through appsettings."
    - "Generator runtime features (scraping/export/paths/db) read from validated appsettings options."
  artifacts:
    - path: "src/Atrocidades.Generator.Api/Controllers/CasesEvidenceController.cs"
      provides: "Evidence association endpoints"
      exports: ["POST /api/cases/{id}/evidence", "DELETE /api/cases/{id}/evidence/{evidenceId}"]
    - path: "src/Atrocidades.Generator.Api/Controllers/CasesTagsController.cs"
      provides: "Tag association endpoints"
      exports: ["POST /api/cases/{id}/tags", "DELETE /api/cases/{id}/tags/{tagId}"]
    - path: "src/Atrocidades.Generator.Api/Services/Export/SnapshotExportService.cs"
      provides: "PostgreSQL snapshot generation"
      contains: "pg_dump|Process"
    - path: "src/Atrocidades.Generator.Api/Services/Export/SnapshotVersionService.cs"
      provides: "Version naming logic"
      contains: "v"
  key_links:
    - from: "Program.cs"
      to: "GeneratorOptions"
      via: "options binding + validation"
      pattern: "AddOptions|Validate"
    - from: "SnapshotExportService"
      to: "SnapshotVersionService"
      via: "output file naming"
      pattern: "GetNextVersion"
    - from: "Evidence/Tags Controllers"
      to: "Case aggregate"
      via: "EF Core relationship updates"
      pattern: "Include|SaveChanges"
---

<objective>
Finalize generator core with metadata association endpoints (evidence/tags), snapshot export/versioning, and appsettings-driven runtime configuration.

Purpose: Enable full curator data enrichment and produce distributable database artifacts for downstream reader consumption.
Output: Evidence/tag APIs, export services with versioning, option binding/validation, and export metadata tests.
</objective>

<execution_context>
@/home/eduardo/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/eduardo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-generator-core/02-02-SUMMARY.md
@.planning/phases/02-generator-core/02-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement case evidence and tag association APIs</name>
  <files>
    src/Atrocidades.Generator.Api/Controllers/CasesEvidenceController.cs
    src/Atrocidades.Generator.Api/Controllers/CasesTagsController.cs
    tests/Atrocidades.Generator.Api.Tests/Cases/CasesMetadataTests.cs
  </files>
  <action>Create API endpoints to attach/detach evidence links/documents and tags to existing cases using EF Core relationship updates. Enforce duplicate-prevention and entity-existence checks. Keep association changes integrated with curation/audit conventions established in prior plans.</action>
  <verify>dotnet test tests/Atrocidades.Generator.Api.Tests --filter "FullyQualifiedName~CasesMetadataTests"</verify>
  <done>Curators can manage evidence and tag links per case, with duplicate associations blocked and invalid identifiers rejected.</done>
</task>

<task type="auto">
  <name>Task 2: Build SQL snapshot export and sequential versioning services</name>
  <files>
    src/Atrocidades.Generator.Api/Services/Export/SnapshotExportService.cs
    src/Atrocidades.Generator.Api/Services/Export/SnapshotVersionService.cs
    tests/Atrocidades.Generator.Api.Tests/Export/SnapshotExportServiceTests.cs
  </files>
  <action>Implement export service that compiles current PostgreSQL database into SQL snapshot files using configured paths and db connection settings. Add version resolver that creates sequential file names (v1.sql, v2.sql, ...), including safe handling when existing versions are present. Ensure export operation surfaces clear failures when tooling or permissions are missing.</action>
  <verify>dotnet test tests/Atrocidades.Generator.Api.Tests --filter "FullyQualifiedName~SnapshotExportServiceTests"</verify>
  <done>Export command generates valid versioned SQL snapshot files and increments versions correctly across repeated runs.</done>
</task>

<task type="auto">
  <name>Task 3: Wire generator runtime to appsettings options with startup validation</name>
  <files>
    src/Atrocidades.Generator.Api/Options/GeneratorOptions.cs
    src/Atrocidades.Generator.Api/Program.cs
    src/Atrocidades.Generator.Api/Services/Discovery/RssAggregatorService.cs
    src/Atrocidades.Generator.Api/Services/Discovery/RedditThreadScraperService.cs
    src/Atrocidades.Generator.Api/Services/Export/SnapshotExportService.cs
  </files>
  <action>Define strongly-typed options for database/export/discovery settings and bind them from appsettings.json with startup validation to fail fast on missing critical values. Refactor scraping/export services to consume injected options instead of hardcoded values. Keep configuration source centralized in appsettings per project decision.</action>
  <verify>dotnet test tests/Atrocidades.Generator.Api.Tests && dotnet build src/Atrocidades.Generator.Api</verify>
  <done>Generator services read required settings from validated appsettings options and application startup fails clearly when required configuration is absent.</done>
</task>

</tasks>

<verification>
Run full generator API tests plus one end-to-end snapshot export smoke run using local PostgreSQL. Confirm output file version increments and evidence/tag endpoints operate on seeded case data.
</verification>

<success_criteria>
- Curators can attach evidence and tags to cases via API.
- SQL snapshot export produces versioned artifacts suitable for distribution.
- Core runtime behaviors are configuration-driven via appsettings with validation.
</success_criteria>

<output>
After completion, create `.planning/phases/02-generator-core/02-05-SUMMARY.md`
</output>
