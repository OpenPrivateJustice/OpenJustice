---
phase: 01-database-configuration-foundation
plan: 04
type: execute
wave: 4
depends_on:
  - 01-02
gap_closure: true
files_modified:
  - src/OpenJustice.Generator/appsettings.json
  - src/OpenJustice.Generator/appsettings.Development.json
  - src/OpenJustice.Generator/Configuration/AppConfiguration.cs
autonomous: true
requirements:
  - CFG-01
  - CFG-02
  - CFG-03
  - CFG-04
  - CFG-05
  - CFG-06
must_haves:
  truths:
    - "appsettings.json configured for database, file paths, and torrent settings"
    - "Configuration validation prevents startup with missing required settings"
  artifacts:
    - path: "src/OpenJustice.Generator/appsettings.json"
      provides: "Main configuration file with PostgreSQL, file paths, and torrent settings"
    - path: "src/OpenJustice.Generator/appsettings.Development.json"
      provides: "Development-specific configuration overrides"
    - path: "src/OpenJustice.Generator/Configuration/AppConfiguration.cs"
      provides: "Strongly-typed configuration class with validation"
  key_links:
    - from: "appsettings.json"
      to: "AppDbContext connection string"
      via: "Configuration binding"
    - from: "appsettings.json"
      to: "File export paths"
      via: "IConfiguration.GetSection"
    - from: "appsettings.json"
      to: "Torrent client"
      via: "IConfiguration.GetSection"
---

<objective>
Create configuration system with appsettings.json, appsettings.Development.json, and validation.

Purpose: Resolve CFG-01 through CFG-06 orphaned requirements - no appsettings.json exists.
</objective>

<execution_context>
@/home/eduardo/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/eduardo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-database-configuration-foundation/01-VERIFICATION.md
@.planning/phases/01-database-configuration-foundation/01-01-SUMMARY.md
@.planning/phases/01-database-configuration-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create appsettings.json with PostgreSQL, file paths, and torrent settings</name>
  <files>
    src/OpenJustice.Generator/appsettings.json
  </files>
  <action>Create appsettings.json with the following sections:
1. ConnectionStrings - PostgreSQL connection string (CFG-01)
2. FilePaths - Export directories, backup paths (CFG-02)
3. Torrent - Tracker URLs, port settings, download limits (CFG-03)
4. Logging - Default logging configuration

Use sensible defaults that work out of the box with clear placeholder values for secrets.</action>
  <verify>test -f src/OpenJustice.Generator/appsettings.json && grep -q "ConnectionStrings\|FilePaths\|Torrent" src/OpenJustice.Generator/appsettings.json</verify>
  <done>appsettings.json exists with ConnectionStrings, FilePaths, and Torrent sections.</done>
</task>

<task type="auto">
  <name>Task 2: Create appsettings.Development.json for development environment</name>
  <files>
    src/OpenJustice.Generator/appsettings.Development.json
  </files>
  <action>Create appsettings.Development.json with development-specific overrides:
1. More verbose logging (Debug level)
2. Development database connection string (local PostgreSQL)
3. Enable developer exception page
4. Disable production features if needed</action>
  <verify>test -f src/OpenJustice.Generator/appsettings.Development.json && grep -q "Logging" src/OpenJustice.Generator/appsettings.Development.json</verify>
  <done>appsettings.Development.json exists with development-specific configuration.</done>
</task>

<task type="auto">
  <name>Task 3: Create AppConfiguration class with validation</name>
  <files>
    src/OpenJustice.Generator/Configuration/AppConfiguration.cs
  </files>
  <action>Create a strongly-typed configuration class in src/OpenJustice.Generator/Configuration/:
1. AppConfiguration class with properties for ConnectionStrings, FilePaths, Torrent settings
2. Use IOptions pattern for strongly-typed binding
3. Add DataAnnotations validation for required fields (CFG-05)
4. Add XML documentation for all properties (CFG-06)</action>
  <verify>test -f src/OpenJustice.Generator/Configuration/AppConfiguration.cs && grep -q "IOptions\|Required\|DataAnnotations" src/OpenJustice.Generator/Configuration/AppConfiguration.cs</verify>
  <done>AppConfiguration.cs exists with IOptions pattern and validation attributes.</done>
</task>

<task type="auto">
  <name>Task 4: Register configuration in Program.cs and add startup validation</name>
  <files>
    src/OpenJustice.Generator/Program.cs
  </files>
  <action>Update Program.cs to:
1. Add configuration registration (AddOptions, AddConfiguration)
2. Add validation to fail fast on startup if required config is missing (CFG-05)
3. Ensure appsettings.json is loaded correctly</action>
  <verify>grep -q "AddOptions\|AddConfiguration\|ValidateOnStart" src/OpenJustice.Generator/Program.cs</verify>
  <done>Program.cs registers configuration and validates on startup.</done>
</task>

</tasks>

<verification>
All configuration files exist, AppConfiguration class binds correctly, and startup validation runs.
</verification>

<success_criteria>
- appsettings.json with PostgreSQL, file paths, torrent settings (CFG-01, CFG-02, CFG-03)
- appsettings.Development.json for development environment (CFG-04)
- AppConfiguration with IOptions and validation (CFG-05, CFG-06)
- CFG-01 through CFG-06 requirements satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/01-database-configuration-foundation/01-04-SUMMARY.md`
</output>
