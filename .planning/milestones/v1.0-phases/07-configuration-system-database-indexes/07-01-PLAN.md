---
phase: 07-configuration-system-database-indexes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/OpenJustice.Generator/appsettings.json
  - src/OpenJustice.Generator/appsettings.Development.json
  - src/OpenJustice.Generator/Configuration/AppConfiguration.cs
  - src/OpenJustice.Generator/ServiceCollectionExtensions.cs
  - src/OpenJustice.Generator/Program.cs
  - docs/configuration.md
autonomous: true
requirements:
  - CFG-01
  - CFG-02
  - CFG-03
  - CFG-04
  - CFG-05
  - CFG-06
must_haves:
  truths:
    - "Generator has runtime configuration files for database, file paths, and torrent settings"
    - "Development environment can override production-safe defaults via appsettings.Development.json"
    - "Application startup fails fast when required configuration is missing or invalid"
    - "Configuration keys and expected values are documented for maintainers"
  artifacts:
    - path: "src/OpenJustice.Generator/appsettings.json"
      provides: "Base runtime settings for PostgreSQL, file paths, torrent, and logging"
      contains: "ConnectionStrings, FilePaths, Torrent"
    - path: "src/OpenJustice.Generator/appsettings.Development.json"
      provides: "Development overrides for local setup"
      contains: "ConnectionStrings and Logging overrides"
    - path: "src/OpenJustice.Generator/Configuration/AppConfiguration.cs"
      provides: "Strongly typed options + validation rules for required fields"
      contains: "AppOptions and AppConfigurationValidator"
    - path: "src/OpenJustice.Generator/Program.cs"
      provides: "Composition root that loads and validates configuration on startup"
      contains: "AddOpenJusticeConfiguration and ValidateOnStart"
    - path: "docs/configuration.md"
      provides: "Human-readable documentation of all config options and local setup"
      contains: "section-by-section key descriptions"
  key_links:
    - from: "src/OpenJustice.Generator/appsettings.json"
      to: "src/OpenJustice.Generator/Configuration/AppConfiguration.cs"
      via: "IOptions binding"
      pattern: "GetSection|Configure<"
    - from: "src/OpenJustice.Generator/ServiceCollectionExtensions.cs"
      to: "src/OpenJustice.Generator/Program.cs"
      via: "service registration at startup"
      pattern: "AddOpenJusticeConfiguration"
    - from: "docs/configuration.md"
      to: "appsettings keys"
      via: "documented key names matching runtime config"
      pattern: "ConnectionStrings|FilePaths|Torrent"
---

<objective>
Close CFG-01 through CFG-06 by making Generator configuration complete, validated at startup, and documented.

Purpose: remove orphaned configuration requirements and make local/production setup reproducible without guessing keys.
Output: validated appsettings files, wired startup configuration validation, and configuration documentation.
</objective>

<execution_context>
@/home/eduardo/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/eduardo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-database-configuration-foundation/01-04-SUMMARY.md
@src/OpenJustice.Generator/appsettings.json
@src/OpenJustice.Generator/Configuration/AppConfiguration.cs
@src/OpenJustice.Generator/ServiceCollectionExtensions.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize appsettings files to the required CFG contract</name>
  <files>
    src/OpenJustice.Generator/appsettings.json
    src/OpenJustice.Generator/appsettings.Development.json
  </files>
  <action>Ensure appsettings.json contains explicit sections for ConnectionStrings (PostgreSQL), FilePaths (snapshot/export/backup/temp), and Torrent (trackers, listen port, transfer limits), with safe placeholder values where secrets are needed. Ensure appsettings.Development.json overrides local defaults (local DB and verbose logging) without removing required keys. Keep key names stable to avoid breaking existing options binding.</action>
  <verify>dotnet build OpenJustice.sln && grep -q '"ConnectionStrings"' src/OpenJustice.Generator/appsettings.json && grep -q '"FilePaths"' src/OpenJustice.Generator/appsettings.json && grep -q '"Torrent"' src/OpenJustice.Generator/appsettings.json</verify>
  <done>Both appsettings files exist and contain all required CFG sections with environment-appropriate values.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce fail-fast startup validation via options wiring</name>
  <files>
    src/OpenJustice.Generator/Configuration/AppConfiguration.cs
    src/OpenJustice.Generator/ServiceCollectionExtensions.cs
    src/OpenJustice.Generator/Program.cs
  </files>
  <action>Keep AppOptions/AppConfigurationValidator as the single source of required configuration rules and wire them into startup so invalid/missing settings fail at application boot (not lazily at first use). If Program.cs is absent, create it as the Generator composition root and register AddOpenJusticeConfiguration plus options validation with ValidateOnStart. Avoid duplicate validation paths; use one predictable startup path.</action>
  <verify>grep -R "ValidateOnStart\|IValidateOptions<AppOptions>\|AddOpenJusticeConfiguration" src/OpenJustice.Generator -n && dotnet build OpenJustice.sln</verify>
  <done>Startup configuration validation is wired and build passes; missing required config would fail during application startup.</done>
</task>

<task type="auto">
  <name>Task 3: Document all configuration options and expected values</name>
  <files>
    docs/configuration.md
  </files>
  <action>Create docs/configuration.md documenting every supported configuration section and key (ConnectionStrings, FilePaths, Torrent, plus existing related sections such as Database/Export/Discovery). For each key, include purpose, expected type/example value, and whether it is required. Include a minimal "local development" section showing how appsettings.Development.json should override base settings.</action>
  <verify>test -f docs/configuration.md && grep -q "ConnectionStrings" docs/configuration.md && grep -q "FilePaths" docs/configuration.md && grep -q "Torrent" docs/configuration.md</verify>
  <done>Configuration documentation exists and maps 1:1 to runtime keys used by the Generator.</done>
</task>

</tasks>

<verification>
Run `dotnet build OpenJustice.sln` and confirm configuration startup wiring, appsettings key presence, and docs coverage for all required configuration sections.
</verification>

<success_criteria>
- CFG-01/02/03: appsettings.json contains DB connection, file paths, torrent settings
- CFG-04: appsettings.Development.json exists and overrides local development values
- CFG-05: startup validation is fail-fast for missing required settings
- CFG-06: configuration keys and usage are documented in docs/configuration.md
</success_criteria>

<output>
After completion, create `.planning/phases/07-configuration-system-database-indexes/07-01-SUMMARY.md`
</output>
