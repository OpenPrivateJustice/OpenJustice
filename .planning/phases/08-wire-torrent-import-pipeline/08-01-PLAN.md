---
phase: 08-wire-torrent-import-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/AtrocidadesRSS.Generator/Services/Export/SnapshotExportService.cs
  - tests/AtrocidadesRSS.Generator.Tests/Export/SnapshotExportServiceTests.cs
autonomous: true
requirements:
  - GEN-15
must_haves:
  truths:
    - "Generator snapshot export emits SQL insert statements consumable by Reader import pipeline."
    - "Exported SQL contains inserts for \"Cases\" and \"CaseFieldHistory\" tables with stable naming contract."
    - "Snapshot export behavior is protected by automated tests so future changes do not break Reader ingestion."
  artifacts:
    - path: "src/AtrocidadesRSS.Generator/Services/Export/SnapshotExportService.cs"
      provides: "pg_dump argument contract for Reader-compatible SQL output"
      contains: "--inserts"
    - path: "tests/AtrocidadesRSS.Generator.Tests/Export/SnapshotExportServiceTests.cs"
      provides: "Contract tests for snapshot export SQL compatibility"
      contains: "Cases"
  key_links:
    - from: "SnapshotExportService.BuildPgDumpArguments()"
      to: "Reader SqliteCaseStore SQL parser"
      via: "pg_dump output format flags"
      pattern: "--inserts|--column-inserts"
    - from: "SnapshotExportServiceTests"
      to: "SnapshotExportService"
      via: "assertions on generated export arguments/contract"
      pattern: "BuildPgDumpArguments|ExportAsync"
---

<objective>
Harden Generator snapshot export so SQL format and table naming are explicitly compatible with Reader SQL import.

Purpose: Eliminate Generator→Reader contract drift that blocks real torrent snapshot ingestion.
Output: Updated export command contract + tests that lock the SQL compatibility behavior.
</objective>

<execution_context>
@/home/eduardo/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/eduardo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@src/AtrocidadesRSS.Generator/Services/Export/SnapshotExportService.cs
@tests/AtrocidadesRSS.Generator.Tests/Export/SnapshotExportServiceTests.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce Reader-compatible pg_dump SQL output contract</name>
  <files>src/AtrocidadesRSS.Generator/Services/Export/SnapshotExportService.cs</files>
  <action>Update snapshot export arguments to produce INSERT-based SQL (not COPY-only output) and preserve deterministic column/value structure for Reader parser compatibility. Keep existing clean/if-exists/no-owner/no-privileges behavior. Ensure argument composition is centralized in BuildPgDumpArguments and documented inline as a Reader contract safeguard for Phase 8 integration.</action>
  <verify>dotnet build src/AtrocidadesRSS.Generator/AtrocidadesRSS.Generator.csproj</verify>
  <done>Snapshot export command includes explicit INSERT output flags and continues to build without runtime regression.</done>
</task>

<task type="auto">
  <name>Task 2: Add export contract tests for Cases/CaseFieldHistory SQL compatibility</name>
  <files>tests/AtrocidadesRSS.Generator.Tests/Export/SnapshotExportServiceTests.cs</files>
  <action>Add/extend tests to lock SQL export compatibility expectations: generated pg_dump arguments include INSERT output, and contract assumptions for Cases and CaseFieldHistory are explicitly asserted so future export refactors cannot silently break Reader import. Keep tests isolated (no real pg_dump execution required).</action>
  <verify>dotnet test tests/AtrocidadesRSS.Generator.Tests/AtrocidadesRSS.Generator.Tests.csproj --filter "SnapshotExportServiceTests"</verify>
  <done>Automated tests fail if export format drifts away from Reader-compatible SQL contract.</done>
</task>

</tasks>

<verification>
Run generator build and targeted export tests; confirm snapshot export contract is now explicit and test-protected.
</verification>

<success_criteria>
- GEN-15 remains functional with Reader-compatible SQL output guarantees.
- Export SQL contract for Cases/CaseFieldHistory is explicit and regression-tested.
</success_criteria>

<output>
After completion, create `.planning/phases/08-wire-torrent-import-pipeline/08-01-SUMMARY.md`
</output>
