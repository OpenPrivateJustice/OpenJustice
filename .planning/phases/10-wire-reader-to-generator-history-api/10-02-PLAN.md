---
phase: 10-wire-reader-to-generator-history-api
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - src/OpenJustice.Reader/Services/Cases/ICaseHistoryService.cs
  - src/OpenJustice.Reader/Services/Cases/CaseHistoryService.cs
  - src/OpenJustice.Reader/Pages/Cases/CaseHistory.razor
  - src/OpenJustice.Reader/Pages/Cases/CaseDetails.razor
autonomous: true
requirements:
  - RDR-22
  - RDR-23
must_haves:
  truths:
    - "History timeline in Reader is loaded from live Generator API data, not from SqliteCaseStore history tables."
    - "Diff comparison (A/B) operates on Generator-returned field history entries."
    - "Confidence badges in timeline/diff reflect ChangeConfidence values from Generator responses."
    - "Unauthorized history requests surface a clear session-expired path instead of silently rendering empty history."
  artifacts:
    - path: "src/OpenJustice.Reader/Services/Cases/CaseHistoryService.cs"
      provides: "History orchestration over GeneratorHistoryApiClient with existing timeline/diff semantics preserved."
      contains: "IGeneratorHistoryApiClient"
    - path: "src/OpenJustice.Reader/Pages/Cases/CaseHistory.razor"
      provides: "UI wiring for live timeline/diff loading and unauthorized error handling."
      contains: "LoadDataAsync"
    - path: "src/OpenJustice.Reader/Pages/Cases/CaseDetails.razor"
      provides: "History link behavior aligned with live API-backed history availability checks."
      contains: "HasHistory"
  key_links:
    - from: "src/OpenJustice.Reader/Services/Cases/CaseHistoryService.cs"
      to: "src/OpenJustice.Reader/Services/Cases/GeneratorHistoryApiClient.cs"
      via: "GetFullTimelineAsync / GetFieldTimelineAsync"
      pattern: "GetCaseHistoryAsync"
    - from: "src/OpenJustice.Reader/Pages/Cases/CaseHistory.razor"
      to: "src/OpenJustice.Reader/Services/Cases/CaseHistoryService.cs"
      via: "LoadDataAsync and LoadDiff"
      pattern: "CaseHistoryService"
    - from: "src/OpenJustice.Reader/Pages/Cases/CaseDetails.razor"
      to: "src/OpenJustice.Reader/Services/Cases/CaseHistoryService.cs"
      via: "HasHistoryAsync check for history navigation"
      pattern: "HasHistoryAsync"
---

<objective>
Switch Reader history features from local snapshot history parsing to live Generator API consumption while preserving existing timeline/diff UX and confidence visualization.

Purpose: Complete the integration so Reader shows authoritative, current history from Generator and handles auth failures predictably.
Output: Refactored CaseHistoryService + UI pages wired to live history responses.
</objective>

<execution_context>
@/home/eduardo/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/eduardo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-wire-reader-to-generator-history-api/10-01-SUMMARY.md
@src/OpenJustice.Reader/Services/Cases/ICaseHistoryService.cs
@src/OpenJustice.Reader/Services/Cases/CaseHistoryService.cs
@src/OpenJustice.Reader/Pages/Cases/CaseHistory.razor
@src/OpenJustice.Reader/Pages/Cases/CaseDetails.razor
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor CaseHistoryService to consume live Generator history API</name>
  <files>src/OpenJustice.Reader/Services/Cases/ICaseHistoryService.cs, src/OpenJustice.Reader/Services/Cases/CaseHistoryService.cs</files>
  <action>Replace direct ILocalCaseStore history reads with IGeneratorHistoryApiClient calls for full timeline, field timeline, available fields, diff selection, and HasHistory. Preserve current deterministic behavior (newest-first ordering and stable A/B diff index semantics). Keep interface signatures stable for page consumers unless a minimal auth-state contract is required; if adding auth-state signaling, do it explicitly and update all consumers in this plan. Do not query SqliteCaseStore history collections in CaseHistoryService after this refactor.</action>
  <verify>dotnet build src/OpenJustice.Reader/OpenJustice.Reader.csproj && grep -n "IGeneratorHistoryApiClient" src/OpenJustice.Reader/Services/Cases/CaseHistoryService.cs</verify>
  <done>CaseHistoryService sources timeline/diff data exclusively from GeneratorHistoryApiClient and maintains existing ordering/diff behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Update history pages to reflect live API data and unauthorized flows</name>
  <files>src/OpenJustice.Reader/Pages/Cases/CaseHistory.razor, src/OpenJustice.Reader/Pages/Cases/CaseDetails.razor</files>
  <action>Update CaseHistory page loading/error states for API-backed history calls so unauthorized responses show a clear "sessão expirada" path (and use configured redirect/login behavior from plan 10-01) instead of silently rendering empty results. Keep the existing timeline cards, diff controls, and confidence badge thresholds unchanged, but ensure they are populated from live API data. Update CaseDetails history-link behavior to avoid false negatives caused by local-history checks (use API-backed HasHistory semantics or always expose navigation when case exists).</action>
  <verify>dotnet build src/OpenJustice.Reader/OpenJustice.Reader.csproj</verify>
  <done>Timeline and diff UI render data returned by Generator API, confidence values remain visible, and unauthorized history calls no longer appear as generic empty history.</done>
</task>

</tasks>

<verification>
1. Build Reader project successfully after service/page refactor.
2. Confirm CaseHistoryService no longer queries local history store methods.
3. Run Reader + Generator locally and verify: loading `/cases/{id}/history` fetches from `/api/cases/{id}/history`; selecting field/diff fetches field timeline data; confidence badges render from API response values.
4. Verify forced 401 response triggers explicit session-expired/login path.
</verification>

<success_criteria>
1. Reader history UI calls Generator CaseHistoryController endpoints for timeline and field history.
2. Timeline and diff views render live Generator data.
3. Confidence score visualization remains intact using Generator-provided ChangeConfidence values.
4. Unauthorized (401) responses are handled explicitly (redirect/session-expired path), not swallowed as empty history.
</success_criteria>

<output>
After completion, create `.planning/phases/10-wire-reader-to-generator-history-api/10-02-SUMMARY.md`
</output>
