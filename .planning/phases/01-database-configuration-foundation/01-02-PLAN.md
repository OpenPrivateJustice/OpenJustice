---
phase: 01-database-configuration-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/AtrocidadesRSS.Generator/Infrastructure/Persistence/Migrations/<timestamp>_InitialDatabaseFoundation.cs
  - src/AtrocidadesRSS.Generator/Infrastructure/Persistence/Migrations/AppDbContextModelSnapshot.cs
  - src/AtrocidadesRSS.Generator/Infrastructure/Persistence/Scripts/ExportSnapshotSql.cs
  - src/AtrocidadesRSS.Generator/Infrastructure/Persistence/Scripts/README.md
autonomous: true
requirements:
  - DB-11
  - DB-12
  - DB-13
  - DB-14
  - DB-15
  - DB-16
  - DB-20
  - DB-21
  - DB-22
must_haves:
  truths:
    - "Database schema can be created/recreated through EF migrations without manual SQL edits."
    - "Index strategy supports the expected search/filter patterns by name, classification, location, date, and status."
    - "A reproducible SQL snapshot export can be generated and imported into PostgreSQL."
  artifacts:
    - path: "src/AtrocidadesRSS.Generator/Infrastructure/Persistence/Migrations/<timestamp>_InitialDatabaseFoundation.cs"
      provides: "Initial migration containing tables, constraints, FKs, and indexes."
    - path: "src/AtrocidadesRSS.Generator/Infrastructure/Persistence/Migrations/AppDbContextModelSnapshot.cs"
      provides: "Canonical EF model snapshot for future schema versioning."
    - path: "src/AtrocidadesRSS.Generator/Infrastructure/Persistence/Scripts/ExportSnapshotSql.cs"
      provides: "Script/utility that outputs importable PostgreSQL SQL snapshot."
  key_links:
    - from: "EF migration"
      to: "PostgreSQL database"
      via: "database update applies full schema"
      pattern: "dotnet ef database update"
    - from: "Migration index definitions"
      to: "case filter/search queries"
      via: "CreateIndex for key columns and combinations"
      pattern: "CreateIndex"
    - from: "Snapshot export script"
      to: "PostgreSQL restore"
      via: "generated .sql file imports without syntax errors"
      pattern: "script.*sql"
---

<objective>
Generate the first durable schema migration, performance indices, and SQL snapshot export flow.

Purpose: Make schema evolution and distribution operational from day one.
Output: Initial migration + model snapshot + export script and usage notes for backup/restore.
</objective>

<execution_context>
@/home/eduardo/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/eduardo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-database-configuration-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create initial EF Core migration with all schema objects and indices</name>
  <files>
    src/AtrocidadesRSS.Generator/Infrastructure/Persistence/Migrations/<timestamp>_InitialDatabaseFoundation.cs
    src/AtrocidadesRSS.Generator/Infrastructure/Persistence/Migrations/AppDbContextModelSnapshot.cs
  </files>
  <action>Run EF Core migration scaffolding after Plan 01 model completion and verify generated migration includes all tables, PK/FK constraints, NOT NULL/defaults, and indexes required by DB-11..DB-16. Ensure index coverage includes accused/victim name lookup, crime/case/status filters, state/location filtering, crime date filtering, and at least one composite index for common multi-filter queries. If generated names are unclear, adjust fluent mappings then regenerate migration instead of hand-editing SQL-heavy blocks.</action>
  <verify>dotnet ef migrations add InitialDatabaseFoundation --project src/AtrocidadesRSS.Generator --startup-project src/AtrocidadesRSS.Generator && grep -n "CreateIndex\|CreateTable" src/AtrocidadesRSS.Generator/Infrastructure/Persistence/Migrations/*InitialDatabaseFoundation.cs</verify>
  <done>Initial migration and snapshot exist and explicitly contain all tables, constraints, and required index definitions.</done>
</task>

<task type="auto">
  <name>Task 2: Validate migration apply/revert flow and document backup/restore baseline</name>
  <files>src/AtrocidadesRSS.Generator/Infrastructure/Persistence/Scripts/README.md</files>
  <action>Execute migration against local PostgreSQL, verify schema materialization, then test rollback/reapply path for confidence in DB-20/DB-21 maintenance. Document exact CLI commands for database update, rollback to 0, and reapply in a persistence scripts README. Keep commands deterministic and compatible with appsettings connection usage.</action>
  <verify>dotnet ef database update --project src/AtrocidadesRSS.Generator --startup-project src/AtrocidadesRSS.Generator && dotnet ef database update 0 --project src/AtrocidadesRSS.Generator --startup-project src/AtrocidadesRSS.Generator && dotnet ef database update --project src/AtrocidadesRSS.Generator --startup-project src/AtrocidadesRSS.Generator</verify>
  <done>Migration lifecycle (apply, rollback, reapply) is proven and maintenance commands are documented for future phases.</done>
</task>

<task type="auto">
  <name>Task 3: Implement SQL snapshot export utility and validate importability</name>
  <files>src/AtrocidadesRSS.Generator/Infrastructure/Persistence/Scripts/ExportSnapshotSql.cs</files>
  <action>Create a script/console utility that generates a versioned SQL snapshot file from the current PostgreSQL schema/data state for distribution (DB-22). Include deterministic output naming (e.g., atro-v{timestamp}.sql), UTF-8 encoding, and explicit command invocation documentation. Validate generated file can be imported into a fresh PostgreSQL database without syntax errors.</action>
  <verify>dotnet run --project src/AtrocidadesRSS.Generator -- export-snapshot-sql && psql -d atrocidadesrss_restore_test -f <generated_snapshot.sql></verify>
  <done>A valid SQL snapshot file is generated and successfully imported into a clean database instance.</done>
</task>

</tasks>

<verification>
Migration creation, apply/revert, and SQL snapshot export/import all run successfully in CLI with no manual intervention.
</verification>

<success_criteria>
- Initial migration can recreate full schema consistently.
- Required indexes are present for all documented filtering/search dimensions.
- Backup/restore baseline commands are documented and tested.
- Snapshot SQL export produces an importable artifact.
</success_criteria>

<output>
After completion, create `.planning/phases/01-database-configuration-foundation/01-02-SUMMARY.md`
</output>
