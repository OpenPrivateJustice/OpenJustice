---
phase: 01-database-configuration-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
gap_closure: true
files_modified:
  - src/OpenJustice.Generator/Infrastructure/Persistence/AppDbContext.cs
  - src/OpenJustice.Generator/Infrastructure/Persistence/Migrations/<timestamp>_AddCompositeIndexes.cs
autonomous: true
requirements:
  - DB-16
must_haves:
  truths:
    - "Composite indexes exist for common multi-column query patterns"
  artifacts:
    - path: "src/OpenJustice.Generator/Infrastructure/Persistence/Migrations/<timestamp>_AddCompositeIndexes.cs"
      provides: "Migration adding composite indexes for multi-column queries"
  key_links:
    - from: "Composite index on (CrimeTypeId, JudicialStatusId)"
      to: "Filtered case queries by crime and status"
      via: "Single index scan instead of multiple"
    - from: "Composite index on (CrimeLocationState, CrimeDate)"
      to: "Location+time queries"
      via: "Single index scan instead of multiple"
---

<objective>
Add composite indexes to support efficient multi-column filter queries.

Purpose: Resolve DB-16 partial gap - currently only single-column indexes exist.
</objective>

<execution_context>
@/home/eduardo/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/eduardo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-database-configuration-foundation/01-VERIFICATION.md
@.planning/phases/01-database-configuration-foundation/01-01-SUMMARY.md
@.planning/phases/01-database-configuration-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add composite indexes to AppDbContext for multi-column queries</name>
  <files>
    src/OpenJustice.Generator/Infrastructure/Persistence/AppDbContext.cs
  </files>
  <action>Add composite indexes using EF Core's HasIndex fluent API for the Cases table:
1. Composite index on (CrimeTypeId, JudicialStatusId) - for filtered case queries
2. Composite index on (CrimeLocationState, CrimeDate) - for location+time queries
Use the same index naming convention as existing indexes (IX_TableName_Column1_Column2).</action>
  <verify>grep -n "HasIndex" src/OpenJustice.Generator/Infrastructure/Persistence/AppDbContext.cs | grep -E "CrimeTypeId.*JudicialStatusId|CrimeLocationState.*CrimeDate"</verify>
  <done>AppDbContext contains composite index configurations for both (CrimeTypeId, JudicialStatusId) and (CrimeLocationState, CrimeDate).</done>
</task>

<task type="auto">
  <name>Task 2: Generate migration for composite indexes</name>
  <files>
    src/OpenJustice.Generator/Infrastructure/Persistence/Migrations/<timestamp>_AddCompositeIndexes.cs
    src/OpenJustice.Generator/Infrastructure/Persistence/Migrations/AppDbContextModelSnapshot.cs
  </files>
  <action>Generate EF Core migration to add the composite indexes:
dotnet ef migrations add AddCompositeIndexes --project src/OpenJustice.Generator --startup-project src/OpenJustice.Generator

Verify migration contains CreateIndex calls with multiple columns.</action>
  <verify>grep -n "CreateIndex" src/OpenJustice.Generator/Infrastructure/Persistence/Migrations/*AddCompositeIndexes.cs | wc -l</verify>
  <done>Migration exists and contains CreateIndex operations for composite index definitions.</done>
</task>

</tasks>

<verification>
Migration compiles and contains composite index definitions for both query patterns.
</verification>

<success_criteria>
- Composite index on (CrimeTypeId, JudicialStatusId) created
- Composite index on (CrimeLocationState, CrimeDate) created
- DB-16 requirement satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/01-database-configuration-foundation/01-03-SUMMARY.md`
</output>
