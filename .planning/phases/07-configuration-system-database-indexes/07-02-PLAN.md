---
phase: 07-configuration-system-database-indexes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/OpenJustice.Generator/Infrastructure/Persistence/AppDbContext.cs
  - src/OpenJustice.Generator/Infrastructure/Persistence/Migrations/20260301175321_AddCompositeIndexes.cs
  - src/OpenJustice.Generator/Infrastructure/Persistence/Migrations/AppDbContextModelSnapshot.cs
autonomous: true
requirements:
  - DB-16
must_haves:
  truths:
    - "Combined filters by crime type + judicial status use a dedicated composite index"
    - "Combined filters by state + crime date use a dedicated composite index"
    - "Composite indexes are represented in both model configuration and migration artifacts"
  artifacts:
    - path: "src/OpenJustice.Generator/Infrastructure/Persistence/AppDbContext.cs"
      provides: "EF model-level composite index definitions"
      contains: "HasIndex(e => new { e.CrimeTypeId, e.JudicialStatusId }) and HasIndex(e => new { e.CrimeLocationState, e.CrimeDate })"
    - path: "src/OpenJustice.Generator/Infrastructure/Persistence/Migrations/20260301175321_AddCompositeIndexes.cs"
      provides: "Migration operations creating both composite indexes"
      contains: "CreateIndex for IX_Cases_CrimeTypeId_JudicialStatusId and IX_Cases_CrimeLocationState_CrimeDate"
    - path: "src/OpenJustice.Generator/Infrastructure/Persistence/Migrations/AppDbContextModelSnapshot.cs"
      provides: "Snapshot parity with index definitions"
      contains: "HasDatabaseName for both composite indexes"
  key_links:
    - from: "AppDbContext.cs"
      to: "AddCompositeIndexes migration"
      via: "EF Core migration generation"
      pattern: "HasDatabaseName\(\"IX_Cases_"
    - from: "AddCompositeIndexes migration"
      to: "database schema"
      via: "migration apply"
      pattern: "CreateIndex"
---

<objective>
Close DB-16 by ensuring composite indexes are fully wired in EF model and migration artifacts for combined search filters.

Purpose: guarantee multi-column query performance for high-frequency filter combinations used by Generator and Reader.
Output: finalized composite index model + migration alignment for Cases queries.
</objective>

<execution_context>
@/home/eduardo/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/eduardo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-database-configuration-foundation/01-03-SUMMARY.md
@src/OpenJustice.Generator/Infrastructure/Persistence/AppDbContext.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm and finalize composite index definitions in EF model</name>
  <files>
    src/OpenJustice.Generator/Infrastructure/Persistence/AppDbContext.cs
  </files>
  <action>Ensure Cases entity defines both composite indexes exactly on (CrimeTypeId, JudicialStatusId) and (CrimeLocationState, CrimeDate) with explicit database names IX_Cases_CrimeTypeId_JudicialStatusId and IX_Cases_CrimeLocationState_CrimeDate. Do not replace them with single-column indexes or alternate column order, because query planner behavior depends on left-to-right index ordering.</action>
  <verify>grep -q "IX_Cases_CrimeTypeId_JudicialStatusId" src/OpenJustice.Generator/Infrastructure/Persistence/AppDbContext.cs && grep -q "IX_Cases_CrimeLocationState_CrimeDate" src/OpenJustice.Generator/Infrastructure/Persistence/AppDbContext.cs</verify>
  <done>AppDbContext has both required composite indexes with correct columns and names.</done>
</task>

<task type="auto">
  <name>Task 2: Align migration and snapshot with model-level indexes</name>
  <files>
    src/OpenJustice.Generator/Infrastructure/Persistence/Migrations/20260301175321_AddCompositeIndexes.cs
    src/OpenJustice.Generator/Infrastructure/Persistence/Migrations/AppDbContextModelSnapshot.cs
  </files>
  <action>Ensure migration up/down operations and model snapshot reflect the same two composite indexes from AppDbContext. If drift exists, regenerate migration artifacts so schema history is consistent and re-runnable. Keep migration history clean (no duplicate re-add of identical indexes).</action>
  <verify>dotnet build OpenJustice.sln && grep -q "CreateIndex" src/OpenJustice.Generator/Infrastructure/Persistence/Migrations/20260301175321_AddCompositeIndexes.cs && grep -q "IX_Cases_CrimeTypeId_JudicialStatusId" src/OpenJustice.Generator/Infrastructure/Persistence/Migrations/AppDbContextModelSnapshot.cs</verify>
  <done>Migration + snapshot are in sync with AppDbContext composite index configuration and build passes.</done>
</task>

</tasks>

<verification>
Validate index names and columns in model/migrations, then run `dotnet build OpenJustice.sln` to ensure migration artifacts and persistence layer compile cleanly.
</verification>

<success_criteria>
- DB-16 complete with both required composite indexes present and correctly named
- EF model, migration file, and model snapshot are consistent
- Solution builds successfully after index alignment
</success_criteria>

<output>
After completion, create `.planning/phases/07-configuration-system-database-indexes/07-02-SUMMARY.md`
</output>
