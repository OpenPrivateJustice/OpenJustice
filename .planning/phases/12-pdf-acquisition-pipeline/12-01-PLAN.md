---
phase: 12-pdf-acquisition-pipeline
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/OpenJustice.BrazilExtractor/Models/TjgoPublicationPdfLink.cs
  - src/OpenJustice.BrazilExtractor/Models/TjgoSearchResult.cs
  - src/OpenJustice.BrazilExtractor/Services/Tjgo/ITjgoSearchService.cs
  - src/OpenJustice.BrazilExtractor/Services/Tjgo/TjgoSearchService.cs
  - src/OpenJustice.BrazilExtractor/Configuration/BrazilExtractorOptions.cs
  - src/OpenJustice.BrazilExtractor/Configuration/BrazilExtractorOptionsValidator.cs
  - tests/OpenJustice.Playwright/TjgoConsultaPublicacaoSmokeTests.cs
autonomous: true
requirements:
  - EXTR-06
must_haves:
  truths:
    - "Operator can run one TJGO query and obtain a deterministic list of PDF URLs extracted from the returned result page."
    - "Extractor enforces the configured per-query cap (default 15) while preserving stable DOM order."
    - "Search output exposes enough metadata (counts and cap status) for downstream download orchestration."
  artifacts:
    - path: "src/OpenJustice.BrazilExtractor/Models/TjgoPublicationPdfLink.cs"
      provides: "Typed contract for extracted publication PDF links"
    - path: "src/OpenJustice.BrazilExtractor/Services/Tjgo/TjgoSearchService.cs"
      provides: "Result-page PDF link harvesting with normalization and de-duplication"
    - path: "src/OpenJustice.BrazilExtractor/Models/TjgoSearchResult.cs"
      provides: "Search result payload carrying extracted PDF inventory"
  key_links:
    - from: "src/OpenJustice.BrazilExtractor/Services/Tjgo/TjgoSearchService.cs"
      to: "src/OpenJustice.BrazilExtractor/Models/TjgoPublicationPdfLink.cs"
      via: "anchor parsing and normalized URL mapping"
      pattern: "href|pdf|download"
    - from: "src/OpenJustice.BrazilExtractor/Services/Tjgo/TjgoSearchService.cs"
      to: "src/OpenJustice.BrazilExtractor/Models/TjgoSearchResult.cs"
      via: "PdfLinks and capture metadata assignment"
      pattern: "PdfLinks|Captured|MaxResultsPerQuery"
    - from: "tests/OpenJustice.Playwright/TjgoConsultaPublicacaoSmokeTests.cs"
      to: "TJGO result DOM contract"
      via: "selector assertions for PDF link candidates"
      pattern: "a\[href\]|pdf|download"
---

<objective>
Establish a reliable PDF-link capture contract from TJGO query results before implementing file download persistence.

Purpose: Satisfy EXTR-06 foundation by ensuring link harvesting is deterministic, capped, and observable.
Output: Typed PDF link model + search-service extraction logic + smoke coverage for result-link selectors.
</objective>

<execution_context>
@/home/eduardo/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/eduardo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-extractor-foundation-and-tjgo-search/11-03-SUMMARY.md
@.planning/phases/11-extractor-foundation-and-tjgo-search/11-RESEARCH.md
@src/OpenJustice.BrazilExtractor/Services/Tjgo/TjgoSearchService.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add typed PDF-link capture contract to TJGO search results</name>
  <files>src/OpenJustice.BrazilExtractor/Models/TjgoPublicationPdfLink.cs, src/OpenJustice.BrazilExtractor/Models/TjgoSearchResult.cs</files>
  <action>Create a dedicated model for extracted publication PDF links (normalized URL, source page index, DOM order index, optional display text/process context) and embed it in TjgoSearchResult. Include explicit capture metadata such as total links seen on page, links retained after de-duplication, and whether retention hit MaxResultsPerQuery. Keep list initialization non-null to avoid downstream null guards.</action>
  <verify>`dotnet build src/OpenJustice.BrazilExtractor/OpenJustice.BrazilExtractor.csproj` succeeds and TjgoSearchResult exposes typed PDF inventory members.</verify>
  <done>TJGO search results can carry deterministic, typed PDF-link inventory plus cap/de-dup metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Implement result-page PDF link harvesting in TjgoSearchService</name>
  <files>src/OpenJustice.BrazilExtractor/Services/Tjgo/ITjgoSearchService.cs, src/OpenJustice.BrazilExtractor/Services/Tjgo/TjgoSearchService.cs, src/OpenJustice.BrazilExtractor/Configuration/BrazilExtractorOptions.cs, src/OpenJustice.BrazilExtractor/Configuration/BrazilExtractorOptionsValidator.cs</files>
  <action>After successful form submission, parse result-page anchors and capture only PDF publication targets (suffix/content patterns like `.pdf` or known download routes). Normalize relative URLs against the current page URL, remove duplicates by normalized absolute URL, preserve first-seen DOM order, and cap retained links at MaxResultsPerQuery (default 15 per user decision/state). Update contracts/options as needed but do NOT perform binary downloads in this task.</action>
  <verify>`dotnet run --project src/OpenJustice.BrazilExtractor/OpenJustice.BrazilExtractor.csproj` logs extracted PDF-link counts and completes without extraction exceptions.</verify>
  <done>Each successful search returns a populated (or explicitly empty) PdfLinks collection with deterministic ordering and max-result enforcement.</done>
</task>

<task type="auto">
  <name>Task 3: Extend Playwright smoke contract to guard PDF-link selectors</name>
  <files>tests/OpenJustice.Playwright/TjgoConsultaPublicacaoSmokeTests.cs</files>
  <action>Add a smoke test that reaches result-state DOM and asserts extractor-critical link selectors remain discoverable for PDF capture. Accept either: (a) at least one valid PDF/download anchor candidate, or (b) an explicit no-results state marker. Keep this as contract validation only (no file download and no heavy retries).</action>
  <verify>`dotnet test tests/OpenJustice.Playwright/OpenJustice.Playwright.csproj --filter "FullyQualifiedName~PdfLink"` passes.</verify>
  <done>Automated smoke coverage detects result-link DOM drift before Phase 12 download wiring.</done>
</task>

</tasks>

<verification>
Build extractor, run one live query iteration, and run targeted Playwright smoke tests to confirm PDF links are harvested with deterministic cap behavior.
</verification>

<success_criteria>
EXTR-06 foundation is in place: search output reliably exposes result-page PDF links (up to configured 15) in deterministic order with de-dup/cap metadata.
</success_criteria>

<output>
After completion, create `.planning/phases/12-pdf-acquisition-pipeline/12-01-SUMMARY.md`
</output>
