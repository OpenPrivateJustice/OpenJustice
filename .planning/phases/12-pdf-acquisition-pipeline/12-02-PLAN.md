---
phase: 12-pdf-acquisition-pipeline
plan: "02"
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - src/OpenJustice.BrazilExtractor/Models/PdfDownloadBatchResult.cs
  - src/OpenJustice.BrazilExtractor/Models/TjgoSearchResult.cs
  - src/OpenJustice.BrazilExtractor/Services/Downloads/IPdfDownloadService.cs
  - src/OpenJustice.BrazilExtractor/Services/Downloads/PdfDownloadService.cs
  - src/OpenJustice.BrazilExtractor/Services/Tjgo/TjgoSearchService.cs
  - src/OpenJustice.BrazilExtractor/Services/Jobs/TjgoSearchJob.cs
  - src/OpenJustice.BrazilExtractor/Program.cs
  - src/OpenJustice.BrazilExtractor/appsettings.json
autonomous: true
requirements:
  - EXTR-07
  - EXTR-08
must_haves:
  truths:
    - "Operator can execute extraction and observe at least 30 seconds between query-level TJGO executions (initial submit and pagination transitions), not between individual file saves."
    - "Operator can find all downloaded PDFs on local disk with unique file names and no overwrites."
    - "Extraction logs/report show query/page counts, download counts, and saved file paths for auditability."
  artifacts:
    - path: "src/OpenJustice.BrazilExtractor/Services/Downloads/PdfDownloadService.cs"
      provides: "PDF persistence pipeline with collision-safe unique naming"
    - path: "src/OpenJustice.BrazilExtractor/Services/Tjgo/TjgoSearchService.cs"
      provides: "Pagination-aware acquisition flow with query-interval throttling"
    - path: "src/OpenJustice.BrazilExtractor/Services/Jobs/TjgoSearchJob.cs"
      provides: "End-to-end orchestration and operational reporting for acquisition"
  key_links:
    - from: "src/OpenJustice.BrazilExtractor/Services/Tjgo/TjgoSearchService.cs"
      to: "src/OpenJustice.BrazilExtractor/Services/Downloads/IPdfDownloadService.cs"
      via: "download invocation for harvested PDF links"
      pattern: "Download|SaveAs|Pdf"
    - from: "src/OpenJustice.BrazilExtractor/Services/Downloads/PdfDownloadService.cs"
      to: "BrazilExtractor:DownloadPath"
      via: "filesystem writes using unique naming and CreateNew semantics"
      pattern: "DownloadPath|FileMode.CreateNew|.pdf"
    - from: "src/OpenJustice.BrazilExtractor/Services/Tjgo/TjgoSearchService.cs"
      to: "BrazilExtractor:QueryIntervalSeconds"
      via: "delay between query/pagination executions"
      pattern: "QueryIntervalSeconds|Task.Delay"
---

<objective>
Implement end-to-end PDF acquisition: query-level throttling plus collision-safe local persistence for all harvested links.

Purpose: Deliver EXTR-07 and EXTR-08 while preserving the locked deterministic cadence (15 PDFs/query, 30s between query executions).
Output: Download service, pagination/query pacing integration, and observable acquisition summaries.
</objective>

<execution_context>
@/home/eduardo/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/eduardo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-pdf-acquisition-pipeline/12-01-SUMMARY.md
@src/OpenJustice.BrazilExtractor/Services/Jobs/TjgoSearchJob.cs
@src/OpenJustice.BrazilExtractor/Worker.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PDF download persistence service with collision-safe unique naming</name>
  <files>src/OpenJustice.BrazilExtractor/Models/PdfDownloadBatchResult.cs, src/OpenJustice.BrazilExtractor/Services/Downloads/IPdfDownloadService.cs, src/OpenJustice.BrazilExtractor/Services/Downloads/PdfDownloadService.cs, src/OpenJustice.BrazilExtractor/Program.cs</files>
  <action>Create a dedicated download service that receives harvested PDF links and persists files under BrazilExtractor:DownloadPath using deterministic unique filenames (include query date + stable identifier/hash + sequence) and `FileMode.CreateNew` to prevent accidental overwrite. Return batch summary metadata (attempted/succeeded/failed + file paths + failure reasons) for job reporting. Keep implementation within .NET/Playwright stack already in repo (no new third-party download libraries).</action>
  <verify>`dotnet build src/OpenJustice.BrazilExtractor/OpenJustice.BrazilExtractor.csproj` succeeds and DI resolves IPdfDownloadService.</verify>
  <done>Extractor has a reusable download boundary that guarantees unique PDF file creation and explicit result reporting.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce query-level 30s cadence and integrate PDF downloads into TJGO acquisition flow</name>
  <files>src/OpenJustice.BrazilExtractor/Models/TjgoSearchResult.cs, src/OpenJustice.BrazilExtractor/Services/Tjgo/TjgoSearchService.cs, src/OpenJustice.BrazilExtractor/Services/Jobs/TjgoSearchJob.cs, src/OpenJustice.BrazilExtractor/appsettings.json</files>
  <action>Refactor TJGO acquisition flow to process result pages/pagination deterministically: harvest PDF links per page (from 12-01), enforce at least QueryIntervalSeconds between query executions (initial submit and each pagination transition), and download PDFs for each harvested batch without per-file throttle delays. Extend result/job output to include timestamps of query actions, pages traversed, download totals, and persisted file paths so operators can verify cadence semantics. Keep cadence behavior aligned with roadmap truth: interval applies to query/pagination actions, not each individual PDF.</action>
  <verify>Run extractor with logs captured (`dotnet run --project src/OpenJustice.BrazilExtractor/OpenJustice.BrazilExtractor.csproj`) and confirm: (1) query/pagination timestamps are >=30s apart, (2) downloaded PDFs exist under `downloads/`, (3) no duplicate filenames are produced in a single run.</verify>
  <done>End-to-end run captures, downloads, and stores PDFs with enforced query-level throttling and collision-free persistence.</done>
</task>

<task type="auto">
  <name>Task 3: Surface acquisition telemetry in worker-level execution logs</name>
  <files>src/OpenJustice.BrazilExtractor/Services/Jobs/TjgoSearchJob.cs</files>
  <action>Update job completion logging to emit concise acquisition telemetry required for operations: query date window, number of query executions/pages, links captured, files downloaded, failed downloads, and destination directory. Ensure failure logs include actionable reasons (HTTP status, IO collision fallback, parsing issues) instead of generic exceptions.</action>
  <verify>Single worker iteration log includes all acquisition telemetry fields and distinguishes success vs partial-failure outcomes.</verify>
  <done>Operators can validate EXTR-07/08 from structured logs without inspecting source code.</done>
</task>

</tasks>

<verification>
Run one end-to-end extractor iteration and validate logs + filesystem outcomes: query-level cadence >=30s, PDF files saved locally, and zero filename collisions.
</verification>

<success_criteria>
Phase 12 goal is met for cadence/storage: extractor downloads TJGO PDFs and persists them locally with unique names while enforcing deterministic 30-second spacing between query/pagination executions.
</success_criteria>

<output>
After completion, create `.planning/phases/12-pdf-acquisition-pipeline/12-02-SUMMARY.md`
</output>
