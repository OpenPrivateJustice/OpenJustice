---
phase: 04-reader-core
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/AtrocidadesRSS.Reader/Services/Sync/ITorrentSyncService.cs
  - src/AtrocidadesRSS.Reader/Services/Sync/TorrentSyncService.cs
  - src/AtrocidadesRSS.Reader/Services/Sync/IVersionService.cs
  - src/AtrocidadesRSS.Reader/Services/Sync/VersionService.cs
  - src/AtrocidadesRSS.Reader/Services/Data/ILocalCaseStore.cs
  - src/AtrocidadesRSS.Reader/Services/Data/SqliteCaseStore.cs
  - src/AtrocidadesRSS.Reader/Pages/Sync.razor
  - src/AtrocidadesRSS.Reader/Program.cs
autonomous: true
requirements:
  - RDR-02
  - RDR-03
  - RDR-04
must_haves:
  truths:
    - "User can download the latest dataset snapshot via torrent workflow"
    - "User can check whether a newer dataset version exists"
    - "Downloaded SQL snapshot is loaded into a local queryable store"
  artifacts:
    - path: "src/AtrocidadesRSS.Reader/Services/Sync/TorrentSyncService.cs"
      provides: "Torrent download orchestration"
    - path: "src/AtrocidadesRSS.Reader/Services/Sync/VersionService.cs"
      provides: "Version check and update decision"
    - path: "src/AtrocidadesRSS.Reader/Services/Data/SqliteCaseStore.cs"
      provides: "Local SQL loading and query access"
  key_links:
    - from: "src/AtrocidadesRSS.Reader/Pages/Sync.razor"
      to: "src/AtrocidadesRSS.Reader/Services/Sync/ITorrentSyncService.cs"
      via: "sync action button"
      pattern: "Download|Sync"
    - from: "src/AtrocidadesRSS.Reader/Services/Sync/TorrentSyncService.cs"
      to: "src/AtrocidadesRSS.Reader/Services/Data/ILocalCaseStore.cs"
      via: "post-download import"
      pattern: "Load|Import"
---

<objective>
Deliver the Reader data synchronization pipeline: torrent download, version check, and local SQL load.

Purpose: Make the public reader usable offline with real dataset snapshots.
Output: Sync services, local store implementation, and Sync UI page.
</objective>

<execution_context>
@/home/eduardo/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/eduardo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/AtrocidadesRSS.Generator/Configuration/AppConfiguration.cs
@src/AtrocidadesRSS.Generator/appsettings.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement torrent sync + version check services</name>
  <files>src/AtrocidadesRSS.Reader/Services/Sync/ITorrentSyncService.cs, src/AtrocidadesRSS.Reader/Services/Sync/TorrentSyncService.cs, src/AtrocidadesRSS.Reader/Services/Sync/IVersionService.cs, src/AtrocidadesRSS.Reader/Services/Sync/VersionService.cs, src/AtrocidadesRSS.Reader/Program.cs</files>
  <action>Implement service interfaces and concrete services for (a) checking available dataset version and (b) downloading snapshot via torrent metadata configured in ReaderOptions. Ensure version check is idempotent and compares current local version versus remote version manifest. Register these services in DI and keep network logic encapsulated in services (not in Razor pages).</action>
  <verify>dotnet build src/AtrocidadesRSS.Reader/AtrocidadesRSS.Reader.csproj</verify>
  <done>Reader exposes callable services that report update availability and trigger torrent download flow without UI coupling.</done>
</task>

<task type="auto">
  <name>Task 2: Create local SQL ingestion store</name>
  <files>src/AtrocidadesRSS.Reader/Services/Data/ILocalCaseStore.cs, src/AtrocidadesRSS.Reader/Services/Data/SqliteCaseStore.cs, src/AtrocidadesRSS.Reader/Program.cs</files>
  <action>Implement a local case store that imports downloaded SQL snapshot into local SQLite (or equivalent browser-compatible local SQL engine selected for the reader project) and exposes query methods needed by search and details pages. Include import status/result objects and explicit failure messages for invalid SQL or missing snapshot file.</action>
  <verify>dotnet build src/AtrocidadesRSS.Reader/AtrocidadesRSS.Reader.csproj</verify>
  <done>After sync service completes download, local store can load snapshot and report ready state for query operations.</done>
</task>

<task type="auto">
  <name>Task 3: Build Sync page with update and download actions</name>
  <files>src/AtrocidadesRSS.Reader/Pages/Sync.razor, src/AtrocidadesRSS.Reader/Components/Layout/NavMenu.razor</files>
  <action>Create Sync.razor with actions: Check for Updates, Download Latest Snapshot, and Load Local Database. Display current version, remote version, sync status, and errors from services. Keep UI logic thin; call the services created above and show deterministic state transitions (idle/checking/downloading/importing/ready).</action>
  <verify>dotnet build src/AtrocidadesRSS.Reader/AtrocidadesRSS.Reader.csproj</verify>
  <done>User can complete the sync flow from UI and see explicit status for each step.</done>
</task>

</tasks>

<verification>
Start the app and execute: check updates -> download -> load local DB; confirm status transitions and ready state.
</verification>

<success_criteria>
RDR-02, RDR-03, and RDR-04 are satisfied with a complete sync pipeline wired through UI and services.
</success_criteria>

<output>
After completion, create `.planning/phases/04-reader-core/04-02-SUMMARY.md`
</output>
