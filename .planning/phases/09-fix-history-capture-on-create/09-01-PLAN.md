---
phase: 09-fix-history-capture-on-create
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/AtrocidadesRSS.Generator/Services/Cases/CaseWorkflowService.cs
  - tests/AtrocidadesRSS.Generator.Tests/Cases/CaseWorkflowServiceTests.cs
  - tests/AtrocidadesRSS.Generator.Tests/History/CaseHistoryControllerTests.cs
autonomous: true
requirements:
  - GEN-01
  - GEN-19
must_haves:
  truths:
    - "Creating a new case immediately produces CaseFieldHistory entries without requiring a later update."
    - "Initial history entries store null -> current value transitions for tracked fields with timestamp and curator."
    - "History API returns timeline data for a newly created case."
  artifacts:
    - path: "src/AtrocidadesRSS.Generator/Services/Cases/CaseWorkflowService.cs"
      provides: "CreateCaseAsync writes initial append-only history after persistence."
      contains: "AppendChangesAsync"
    - path: "tests/AtrocidadesRSS.Generator.Tests/Cases/CaseWorkflowServiceTests.cs"
      provides: "Regression tests that verify history is created on case creation."
      contains: "CreateCaseAsync"
    - path: "tests/AtrocidadesRSS.Generator.Tests/History/CaseHistoryControllerTests.cs"
      provides: "API-level test proving timeline endpoint returns entries for brand-new case."
      contains: "GetCaseHistory"
  key_links:
    - from: "CaseWorkflowService.CreateCaseAsync"
      to: "ICaseFieldHistoryService.AppendChangesAsync"
      via: "invoke with oldCase = null after initial SaveChangesAsync"
      pattern: "AppendChangesAsync\(.*null"
    - from: "CaseHistoryController.GetCaseHistory"
      to: "AppDbContext.CaseFieldHistories"
      via: "CaseFieldHistoryService.GetHistoryForCaseAsync"
      pattern: "GetHistoryForCaseAsync"
---

<objective>
Ensure new case creation writes initial immutable field history so timeline and diff features work from the first saved version.

Purpose: Close the create→history integration gap in Generator.
Output: Create flow wiring + regression tests proving API timeline works for fresh cases.
</objective>

<execution_context>
@/home/eduardo/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/eduardo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/AtrocidadesRSS.Generator/Services/Cases/CaseWorkflowService.cs
@src/AtrocidadesRSS.Generator/Services/History/CaseFieldHistoryService.cs
@tests/AtrocidadesRSS.Generator.Tests/Cases/CaseWorkflowServiceTests.cs
@tests/AtrocidadesRSS.Generator.Tests/History/CaseHistoryControllerTests.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture initial field history during case creation</name>
  <files>src/AtrocidadesRSS.Generator/Services/Cases/CaseWorkflowService.cs</files>
  <action>In CreateCaseAsync, keep existing FK validation + reference code behavior, then append initial history after the case is persisted and has an ID. Call _fieldHistoryService.AppendChangesAsync(caseEntity.Id, null, caseEntity, request.CuratorId, cancellationToken) so tracked fields are recorded as null -> initial value transitions. Do not change update-path append-only semantics or reference code generation logic.</action>
  <verify>dotnet test tests/AtrocidadesRSS.Generator.Tests --filter "FullyQualifiedName~CaseWorkflowServiceTests"</verify>
  <done>Creating a case results in one or more CaseFieldHistory rows for tracked initialized fields, with ChangedAt/CreatedAt and curator metadata set.</done>
</task>

<task type="auto">
  <name>Task 2: Add regression coverage for create-path history and API timeline</name>
  <files>tests/AtrocidadesRSS.Generator.Tests/Cases/CaseWorkflowServiceTests.cs, tests/AtrocidadesRSS.Generator.Tests/History/CaseHistoryControllerTests.cs</files>
  <action>Add test(s) asserting CreateCaseAsync creates history records without requiring UpdateCaseAsync. Assert at minimum that history for created case is non-empty, includes expected tracked field (e.g., VictimName), and old value is null while new value matches the request payload. Add/adjust controller test proving GET /api/cases/{id}/history returns entries for a newly created case immediately after creation.</action>
  <verify>dotnet test tests/AtrocidadesRSS.Generator.Tests --filter "FullyQualifiedName~CaseWorkflowServiceTests|FullyQualifiedName~CaseHistoryControllerTests"</verify>
  <done>Tests fail if create-path history wiring is removed, and pass when brand-new case timeline is available through history API.</done>
</task>

</tasks>

<verification>
Run: dotnet test tests/AtrocidadesRSS.Generator.Tests
Confirm all history and case workflow tests pass, including new create-path history assertions.
</verification>

<success_criteria>
1. POST case creation path persists initial CaseFieldHistory entries.
2. Initial entries represent null -> value transitions with timestamp/curator metadata.
3. Case history endpoint returns timeline entries for brand-new cases.
4. Regression tests enforce the behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/09-fix-history-capture-on-create/09-01-SUMMARY.md`
</output>
