---
phase: 13-ocr-text-extraction-and-quality-signals
plan: "02"
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - src/OpenJustice.BrazilExtractor/Program.cs
  - src/OpenJustice.BrazilExtractor/Models/TjgoSearchResult.cs
  - src/OpenJustice.BrazilExtractor/Services/Ocr/TesseractOcrExtractionService.cs
  - src/OpenJustice.BrazilExtractor/Services/Jobs/TjgoSearchJob.cs
  - src/OpenJustice.BrazilExtractor/Worker.cs
autonomous: true
requirements:
  - EXTR-10
  - EXTR-12
must_haves:
  truths:
    - "For each successfully downloaded PDF, extractor produces a `.txt` artifact with the same base filename for traceability."
    - "Operator can inspect worker/job logs and see OCR totals (attempted/succeeded/failed) for each extraction iteration."
    - "Operator can review a persistent OCR failure log listing each failed PDF path and failure reason."
  artifacts:
    - path: "src/OpenJustice.BrazilExtractor/Services/Ocr/TesseractOcrExtractionService.cs"
      provides: "TXT artifact persistence and failure-log append behavior"
    - path: "src/OpenJustice.BrazilExtractor/Services/Jobs/TjgoSearchJob.cs"
      provides: "Download-to-OCR orchestration and OCR telemetry logging"
    - path: "src/OpenJustice.BrazilExtractor/Models/TjgoSearchResult.cs"
      provides: "Search result payload including OCR execution summary"
  key_links:
    - from: "src/OpenJustice.BrazilExtractor/Services/Jobs/TjgoSearchJob.cs"
      to: "src/OpenJustice.BrazilExtractor/Services/Ocr/IOcrExtractionService.cs"
      via: "OCR invocation after successful PDF downloads"
      pattern: "ExecuteAsync|DownloadResult|Ocr"
    - from: "src/OpenJustice.BrazilExtractor/Services/Ocr/TesseractOcrExtractionService.cs"
      to: "downloaded PDF file paths"
      via: "Path.ChangeExtension and UTF-8 text write"
      pattern: "Path.ChangeExtension|.txt|UTF8"
    - from: "src/OpenJustice.BrazilExtractor/Worker.cs"
      to: "src/OpenJustice.BrazilExtractor/Models/TjgoSearchResult.cs"
      via: "worker-level OCR telemetry"
      pattern: "OcrResult|SucceededCount|FailedCount|FailureLog"
---

<objective>
Wire OCR into the extraction pipeline so each downloaded PDF yields traceable `.txt` output and operational failure visibility.

Purpose: Deliver EXTR-10 and EXTR-12 by connecting OCR stage execution to the existing TJGO search/download loop.
Output: Runtime OCR orchestration, same-base `.txt` artifacts, worker telemetry, and persistent OCR failure logs.
</objective>

<execution_context>
@/home/eduardo/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/eduardo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-ocr-text-extraction-and-quality-signals/13-01-SUMMARY.md
@src/OpenJustice.BrazilExtractor/Services/Jobs/TjgoSearchJob.cs
@src/OpenJustice.BrazilExtractor/Worker.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire OCR stage after download completion in the TJGO job pipeline</name>
  <files>src/OpenJustice.BrazilExtractor/Program.cs, src/OpenJustice.BrazilExtractor/Models/TjgoSearchResult.cs, src/OpenJustice.BrazilExtractor/Services/Jobs/TjgoSearchJob.cs</files>
  <action>Register the OCR service in DI and invoke it in `TjgoSearchJob` immediately after successful PDF download batches. Process only files that were actually downloaded, attach OCR batch summary to `TjgoSearchResult`, and preserve existing query cadence behavior (30-second query interval remains query-level, not OCR-level). If no downloaded files exist, skip OCR stage with explicit informational logging.</action>
  <verify>`dotnet build src/OpenJustice.BrazilExtractor/OpenJustice.BrazilExtractor.csproj` succeeds and `TjgoSearchJob` logs OCR stage execution when downloaded files are present.</verify>
  <done>Extraction pipeline now includes an OCR stage that executes automatically after PDF acquisition and surfaces structured OCR results in job output.</done>
</task>

<task type="auto">
  <name>Task 2: Persist same-base `.txt` artifacts and append a clear OCR failure log</name>
  <files>src/OpenJustice.BrazilExtractor/Services/Ocr/TesseractOcrExtractionService.cs, src/OpenJustice.BrazilExtractor/Services/Jobs/TjgoSearchJob.cs, src/OpenJustice.BrazilExtractor/Worker.cs</files>
  <action>Update OCR persistence so each successful PDF produces a UTF-8 `.txt` file using the exact same base name (`Path.ChangeExtension(pdfPath, ".txt")`). For failures, append one structured entry per failed PDF to the configured OCR failure log (timestamp, PDF path, language, reason, exception type). Extend worker-level telemetry to report OCR attempted/succeeded/failed totals and failure-log location. Avoid swallowing OCR exceptions silently—every failure must be traceable.</action>
  <verify>Run one controlled iteration (`timeout 90s dotnet run --project src/OpenJustice.BrazilExtractor/OpenJustice.BrazilExtractor.csproj`), then verify: (1) `.txt` artifacts exist with same base names as downloaded PDFs, and (2) failure log file exists and contains entries for OCR failures if any occurred.</verify>
  <done>Operator can trace every OCR outcome: successful PDFs have same-base `.txt` files and failed PDFs appear in a persistent, human-reviewable failure log.</done>
</task>

</tasks>

<verification>
Execute one extractor iteration and validate filesystem + logs: OCR runs automatically after downloads, outputs `.txt` files with matching base names, and records failures in a dedicated log.
</verification>

<success_criteria>
Phase 13 outcomes are met for artifacts and quality visibility: OCR output is generated with Portuguese-ready text files and failure cases are explicitly listed for operator review.
</success_criteria>

<output>
After completion, create `.planning/phases/13-ocr-text-extraction-and-quality-signals/13-02-SUMMARY.md`
</output>
