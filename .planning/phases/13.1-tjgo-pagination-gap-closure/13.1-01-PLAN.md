---
phase: 13.1-tjgo-pagination-gap-closure
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/OpenJustice.BrazilExtractor/Services/Tjgo/TjgoSearchService.cs
  - src/OpenJustice.BrazilExtractor/Models/TjgoSearchResult.cs
  - src/OpenJustice.BrazilExtractor/Services/Jobs/TjgoSearchJob.cs
  - src/OpenJustice.BrazilExtractor/Configuration/BrazilExtractorOptions.cs
  - src/OpenJustice.BrazilExtractor/Configuration/BrazilExtractorOptionsValidator.cs
  - src/OpenJustice.BrazilExtractor/appsettings.json
autonomous: true
gap_closure: true
requirements:
  - EXTR-06
  - EXTR-07
  - EXTR-13
must_haves:
  truths:
    - "A single TJGO search execution traverses all available result pages (not only page 1) and aggregates PDF links across the full pagination chain."
    - "The extractor waits at least QueryIntervalSeconds before each pagination navigation and before each new query execution."
    - "Search output/log telemetry exposes how many pages were traversed and how many links were harvested from the full traversal."
    - "Configuration no longer implies multi-day query windows when the implementation is single-day only."
  artifacts:
    - path: "src/OpenJustice.BrazilExtractor/Services/Tjgo/TjgoSearchService.cs"
      provides: "Pagination loop, multi-pattern next-page detection, per-navigation cadence wait, and full-link aggregation"
    - path: "src/OpenJustice.BrazilExtractor/Models/TjgoSearchResult.cs"
      provides: "Pagination traversal metadata (total pages traversed and related counters)"
    - path: "src/OpenJustice.BrazilExtractor/Services/Jobs/TjgoSearchJob.cs"
      provides: "Job-level telemetry/reporting that reflects full multi-page traversal"
    - path: "src/OpenJustice.BrazilExtractor/Configuration/BrazilExtractorOptions.cs"
      provides: "Single-day query configuration contract without misleading DateWindowDays semantics"
  key_links:
    - from: "src/OpenJustice.BrazilExtractor/Services/Tjgo/TjgoSearchService.cs"
      to: "src/OpenJustice.BrazilExtractor/Configuration/BrazilExtractorOptions.cs"
      via: "QueryIntervalSeconds delay enforced before each page transition"
      pattern: "QueryIntervalSeconds|Task.Delay|pagination"
    - from: "src/OpenJustice.BrazilExtractor/Services/Tjgo/TjgoSearchService.cs"
      to: "src/OpenJustice.BrazilExtractor/Models/TjgoSearchResult.cs"
      via: "Aggregated PdfLinks plus pages-traversed metadata"
      pattern: "PdfLinks|PagesTraversed|PageIndex"
    - from: "src/OpenJustice.BrazilExtractor/Services/Jobs/TjgoSearchJob.cs"
      to: "src/OpenJustice.BrazilExtractor/Models/TjgoSearchResult.cs"
      via: "Download/OCR orchestration consumes full multi-page link set"
      pattern: "result.PdfLinks|PagesTraversed|LogAcquisitionTelemetry"
---

<objective>
Close the pagination gap so TJGO extraction collects PDF links from all result pages while preserving cadence and accurate telemetry.

Purpose: Restore EXTR-06/EXTR-07 behavior under real multi-page result sets and remove misleading date-window configuration semantics.
Output: Pagination-aware search flow, page-traversal reporting, and clarified single-day query configuration.
</objective>

<execution_context>
@/home/eduardo/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/eduardo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-pdf-acquisition-pipeline/12-01-SUMMARY.md
@.planning/phases/12-pdf-acquisition-pipeline/12-02-SUMMARY.md
@src/OpenJustice.BrazilExtractor/Services/Tjgo/TjgoSearchService.cs
@src/OpenJustice.BrazilExtractor/Models/TjgoSearchResult.cs
@src/OpenJustice.BrazilExtractor/Services/Jobs/TjgoSearchJob.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement full pagination traversal and robust next-page detection in TJGO search</name>
  <files>src/OpenJustice.BrazilExtractor/Services/Tjgo/TjgoSearchService.cs</files>
  <action>Refactor the result-harvesting flow to iterate all result pages after the initial submit instead of stopping at page 1. Implement a pagination loop that: (1) extracts PDF links from current page, (2) detects an enabled next-page action using multiple UI patterns (`rel='next'`, `.next`, numbered pager links, `>`/`>>`/`»`, and Portuguese labels like `Próxima`), (3) waits exactly `QueryIntervalSeconds` before each page transition, and (4) navigates and repeats until no valid next action remains. Keep de-duplication by normalized URL across all traversed pages, preserve per-page source index metadata, and remove any global first-page cap behavior that would drop links from later pages.</action>
  <verify>Run one controlled extractor iteration against a known high-volume date and confirm logs show page transitions > 1 with cumulative PDF link counts increasing after each page; `dotnet build src/OpenJustice.BrazilExtractor/OpenJustice.BrazilExtractor.csproj` passes.</verify>
  <done>Search execution no longer truncates at page 1 and can harvest the complete PDF link set available across pagination.</done>
</task>

<task type="auto">
  <name>Task 2: Extend result contracts and job telemetry for multi-page traceability</name>
  <files>src/OpenJustice.BrazilExtractor/Models/TjgoSearchResult.cs, src/OpenJustice.BrazilExtractor/Services/Jobs/TjgoSearchJob.cs</files>
  <action>Add explicit pagination telemetry fields to `TjgoSearchResult` (e.g., total pages traversed, final page index, and aggregated link counters) and populate them from the updated search service. Update `TjgoSearchJob` logging/telemetry so operational output clearly reports query executions, pages traversed, and full link/download/OCR totals from multi-page harvesting. Ensure downstream download stage consumes the full aggregated `PdfLinks` set and does not assume single-page results.</action>
  <verify>`dotnet build src/OpenJustice.BrazilExtractor/OpenJustice.BrazilExtractor.csproj` passes and a runtime iteration log includes `Pages Traversed` with value > 1 when pagination exists.</verify>
  <done>Operators can verify multi-page extraction directly from result payloads/logs, and downstream stages process the complete harvested link inventory.</done>
</task>

<task type="auto">
  <name>Task 3: Clarify single-day date configuration and remove misleading DateWindowDays semantics</name>
  <files>src/OpenJustice.BrazilExtractor/Configuration/BrazilExtractorOptions.cs, src/OpenJustice.BrazilExtractor/Configuration/BrazilExtractorOptionsValidator.cs, src/OpenJustice.BrazilExtractor/appsettings.json</files>
  <action>Align configuration with actual single-day query behavior: remove or deprecate `DateWindowDays` so settings no longer imply multi-day iteration, and update option/validator contracts to make `QueryDateWindowStartDate` the explicit query-date control. Keep existing single-day query semantics unchanged (DataInicial == DataFinal), and ensure startup validation/messages reflect the true behavior.</action>
  <verify>Start extractor with current config (`dotnet run --project src/OpenJustice.BrazilExtractor/OpenJustice.BrazilExtractor.csproj`) and confirm options validation succeeds without `DateWindowDays` while single-day query execution remains unchanged.</verify>
  <done>Configuration is truthful about single-day behavior, reducing operator confusion and preventing false expectations about automatic multi-day coverage.</done>
</task>

</tasks>

<verification>
Run a full extraction iteration using a date with multi-page TJGO results and confirm: (1) all pages are traversed until pagination ends, (2) each page navigation respects QueryIntervalSeconds, and (3) final telemetry reports full-page traversal counts and complete PDF link totals.
</verification>

<success_criteria>
Pagination gap is closed: extractor captures all available PDFs across pages, enforces cadence on both new query and pagination actions, and exposes complete traversal metadata for auditability.
</success_criteria>

<output>
After completion, create `.planning/phases/13.1-tjgo-pagination-gap-closure/13.1-01-SUMMARY.md`
</output>
