---
phase: 02-generator-core
plan: 04
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/Atrocidades.Generator.Api/Services/Discovery/RssAggregatorService.cs
  - src/Atrocidades.Generator.Api/Services/Discovery/RedditThreadScraperService.cs
  - src/Atrocidades.Generator.Api/Controllers/DiscoveryController.cs
  - src/Atrocidades.Generator.Api/Contracts/Discovery/DiscoveredCaseDto.cs
  - src/Atrocidades.Generator.Api/Services/Discovery/DiscoveredCaseReviewService.cs
  - src/Atrocidades.Generator.Api/Domain/Entities/DiscoveredCase.cs
  - tests/Atrocidades.Generator.Api.Tests/Discovery/RssAggregatorServiceTests.cs
  - tests/Atrocidades.Generator.Api.Tests/Discovery/RedditThreadScraperServiceTests.cs
autonomous: true
requirements: [GEN-09, GEN-10, GEN-11]
must_haves:
  truths:
    - "System discovers potential cases from configured RSS feeds."
    - "System extracts potential cases from configured subreddit threads."
    - "Curator can approve or reject auto-discovered items before promotion to official cases."
  artifacts:
    - path: "src/Atrocidades.Generator.Api/Services/Discovery/RssAggregatorService.cs"
      provides: "RSS ingestion and normalization pipeline"
      contains: "HttpClient|Syndication"
    - path: "src/Atrocidades.Generator.Api/Services/Discovery/RedditThreadScraperService.cs"
      provides: "Reddit thread extraction pipeline"
      contains: "Reddit"
    - path: "src/Atrocidades.Generator.Api/Controllers/DiscoveryController.cs"
      provides: "Review endpoints for discovered cases"
      exports: ["GET /api/discovery/cases", "POST /api/discovery/cases/{id}/approve", "POST /api/discovery/cases/{id}/reject"]
  key_links:
    - from: "RssAggregatorService"
      to: "appsettings Discovery options"
      via: "configured feed list"
      pattern: "IOptions"
    - from: "RedditThreadScraperService"
      to: "DiscoveredCase store"
      via: "normalized extraction output"
      pattern: "Add|SaveChanges"
    - from: "DiscoveryController"
      to: "DiscoveredCaseReviewService"
      via: "approve/reject actions"
      pattern: "Approve|Reject"
---

<objective>
Implement automated discovery ingestion (RSS + Reddit) plus curator review workflow for discovered candidates.

Purpose: Reduce manual discovery effort while preserving human curation gate before publication.
Output: Discovery services, review API, discovered-case storage model, and extraction/review tests.
</objective>

<execution_context>
@/home/eduardo/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/eduardo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-generator-core/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build RSS and Reddit discovery ingestion services</name>
  <files>
    src/Atrocidades.Generator.Api/Services/Discovery/RssAggregatorService.cs
    src/Atrocidades.Generator.Api/Services/Discovery/RedditThreadScraperService.cs
    src/Atrocidades.Generator.Api/Domain/Entities/DiscoveredCase.cs
    tests/Atrocidades.Generator.Api.Tests/Discovery/RssAggregatorServiceTests.cs
    tests/Atrocidades.Generator.Api.Tests/Discovery/RedditThreadScraperServiceTests.cs
  </files>
  <action>Implement scheduled/on-demand discovery services that read source definitions from appsettings, fetch RSS/subreddit content, normalize key fields (title, source URL, publication date, summary), and persist deduplicated discovered cases. Keep raw-source traceability fields for curator review. Do not auto-promote discovered items directly into official cases.</action>
  <verify>dotnet test tests/Atrocidades.Generator.Api.Tests --filter "FullyQualifiedName~Discovery"</verify>
  <done>RSS and Reddit sources produce deduplicated discovered-case records with normalized metadata stored for review.</done>
</task>

<task type="auto">
  <name>Task 2: Implement discovered-case approval/rejection endpoints</name>
  <files>
    src/Atrocidades.Generator.Api/Controllers/DiscoveryController.cs
    src/Atrocidades.Generator.Api/Contracts/Discovery/DiscoveredCaseDto.cs
    src/Atrocidades.Generator.Api/Services/Discovery/DiscoveredCaseReviewService.cs
  </files>
  <action>Create endpoints to list pending discovered cases and approve/reject them. Approval should create a draft case record in the curation pipeline (pending approval status) instead of forcing immediate publication. Rejection should capture reason and keep review auditability. Use explicit status transitions to prevent duplicate approvals.</action>
  <verify>dotnet test tests/Atrocidades.Generator.Api.Tests --filter "FullyQualifiedName~Discovery"</verify>
  <done>Curator can reliably approve/reject discovered candidates and system prevents duplicate processing of the same discovered item.</done>
</task>

</tasks>

<verification>
Run discovery-focused test suite and execute a smoke run against mocked RSS/Reddit feeds to confirm ingestion and approval/rejection flows.
</verification>

<success_criteria>
- Automated ingestion works for RSS and Reddit configured sources.
- Discovered items remain gated by curator review.
- Review actions are deterministic and idempotent for already-processed items.
</success_criteria>

<output>
After completion, create `.planning/phases/02-generator-core/02-04-SUMMARY.md`
</output>
