---
phase: 11-extractor-foundation-and-tjgo-search
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - OpenJustice.sln
  - src/OpenJustice.BrazilExtractor/OpenJustice.BrazilExtractor.csproj
  - src/OpenJustice.BrazilExtractor/Program.cs
  - src/OpenJustice.BrazilExtractor/Worker.cs
  - src/OpenJustice.BrazilExtractor/appsettings.json
  - src/OpenJustice.BrazilExtractor/Configuration/BrazilExtractorOptions.cs
  - src/OpenJustice.BrazilExtractor/Configuration/BrazilExtractorOptionsValidator.cs
autonomous: true
requirements:
  - EXTR-01
  - EXTR-13
  - EXTR-14
must_haves:
  truths:
    - "Operator can start OpenJustice.BrazilExtractor without runtime wiring errors."
    - "Extractor startup fails immediately when required configuration is missing or invalid."
    - "Validated extractor settings are available through IOptions to worker services."
  artifacts:
    - path: "src/OpenJustice.BrazilExtractor/OpenJustice.BrazilExtractor.csproj"
      provides: "Worker project targeting net10.0 with required packages"
    - path: "src/OpenJustice.BrazilExtractor/appsettings.json"
      provides: "BrazilExtractor configuration section with required keys"
    - path: "src/OpenJustice.BrazilExtractor/Configuration/BrazilExtractorOptions.cs"
      provides: "Strongly typed settings contract"
    - path: "src/OpenJustice.BrazilExtractor/Configuration/BrazilExtractorOptionsValidator.cs"
      provides: "Fail-fast options validation rules"
    - path: "src/OpenJustice.BrazilExtractor/Program.cs"
      provides: "DI registration with AddOptions + ValidateOnStart"
  key_links:
    - from: "src/OpenJustice.BrazilExtractor/appsettings.json"
      to: "src/OpenJustice.BrazilExtractor/Program.cs"
      via: "Bind(BrazilExtractor section)"
      pattern: "GetSection\(\"BrazilExtractor\"\)"
    - from: "src/OpenJustice.BrazilExtractor/Configuration/BrazilExtractorOptionsValidator.cs"
      to: "src/OpenJustice.BrazilExtractor/Program.cs"
      via: "IValidateOptions registration"
      pattern: "AddSingleton<IValidateOptions<BrazilExtractorOptions>"
---

<objective>
Create the extractor project foundation with validated configuration boundaries so every later TJGO automation step runs from a deterministic host.

Purpose: Satisfy worker/bootstrap and fail-fast settings contracts before portal automation.
Output: A buildable `OpenJustice.BrazilExtractor` project in solution with typed options + startup validation.
</objective>

<execution_context>
@/home/eduardo/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/eduardo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-extractor-foundation-and-tjgo-search/11-RESEARCH.md
@OpenJustice.sln
@src/OpenJustice.Reader/Program.cs
@src/OpenJustice.Generator/ServiceCollectionExtensions.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold BrazilExtractor worker project and solution wiring</name>
  <files>OpenJustice.sln, src/OpenJustice.BrazilExtractor/OpenJustice.BrazilExtractor.csproj, src/OpenJustice.BrazilExtractor/Program.cs, src/OpenJustice.BrazilExtractor/Worker.cs</files>
  <action>Create `OpenJustice.BrazilExtractor` as a .NET 10 worker under `src/` and add it to `OpenJustice.sln`. Keep the standard worker host lifecycle (do not implement custom loops in `Program.cs`) and include `Microsoft.Playwright` plus options-validation dependencies required by this phase. Preserve existing solution folder conventions used by other `src` projects.</action>
  <verify>`dotnet sln OpenJustice.sln list` contains `src/OpenJustice.BrazilExtractor/OpenJustice.BrazilExtractor.csproj` and `dotnet build OpenJustice.sln` succeeds.</verify>
  <done>The repository contains a buildable worker project in solution and startup host wiring compiles without placeholder errors.</done>
</task>

<task type="auto">
  <name>Task 2: Add typed extractor settings and fail-fast options validation</name>
  <files>src/OpenJustice.BrazilExtractor/appsettings.json, src/OpenJustice.BrazilExtractor/Configuration/BrazilExtractorOptions.cs, src/OpenJustice.BrazilExtractor/Configuration/BrazilExtractorOptionsValidator.cs, src/OpenJustice.BrazilExtractor/Program.cs</files>
  <action>Define a `BrazilExtractor` settings section covering TJGO URL, date window defaults, criminal-mode toggle/profile, output/download paths, and query cadence placeholders required by Phase 11-12 continuity. Bind with `AddOptions<BrazilExtractorOptions>()`, register `IValidateOptions<BrazilExtractorOptions>`, and enforce `ValidateOnStart()` so missing/invalid settings fail at startup (do not defer validation to runtime service methods).</action>
  <verify>`dotnet run --project src/OpenJustice.BrazilExtractor/OpenJustice.BrazilExtractor.csproj --no-build` starts with valid config, and intentionally removing one required key from `appsettings.json` causes startup failure with validation message.</verify>
  <done>Extractor config is strongly typed, validated at host startup, and injected through IOptions for downstream services.</done>
</task>

</tasks>

<verification>
Run a clean build and startup check for the new project, then run a negative-config check proving fail-fast validation triggers before browser execution.
</verification>

<success_criteria>
`OpenJustice.BrazilExtractor` exists in solution, compiles on `net10.0`, and refuses to start when required `BrazilExtractor` settings are invalid or missing.
</success_criteria>

<output>
After completion, create `.planning/phases/11-extractor-foundation-and-tjgo-search/11-01-SUMMARY.md`
</output>
