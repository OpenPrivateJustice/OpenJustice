---
phase: 11-extractor-foundation-and-tjgo-search
plan: "02"
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - src/OpenJustice.BrazilExtractor/Models/TjgoSearchQuery.cs
  - src/OpenJustice.BrazilExtractor/Models/TjgoSearchResult.cs
  - src/OpenJustice.BrazilExtractor/Services/Browser/IPlaywrightBrowserFactory.cs
  - src/OpenJustice.BrazilExtractor/Services/Browser/PlaywrightBrowserFactory.cs
  - src/OpenJustice.BrazilExtractor/Services/Tjgo/ITjgoSearchService.cs
  - src/OpenJustice.BrazilExtractor/Services/Tjgo/TjgoSearchService.cs
  - src/OpenJustice.BrazilExtractor/Services/Jobs/TjgoSearchJob.cs
  - src/OpenJustice.BrazilExtractor/Program.cs
  - src/OpenJustice.BrazilExtractor/Worker.cs
autonomous: true
requirements:
  - EXTR-02
  - EXTR-03
  - EXTR-05
must_haves:
  truths:
    - "Operator can run the worker and it launches Chromium through Playwright for TJGO search execution."
    - "Extractor fills and submits ConsultaPublicacao form fields deterministically and reaches result workflow URLs/state."
    - "Single-day queries set DataInicial and DataFinal to the same value and preserve this constraint through submission."
  artifacts:
    - path: "src/OpenJustice.BrazilExtractor/Services/Browser/PlaywrightBrowserFactory.cs"
      provides: "Centralized Playwright + Chromium lifecycle"
    - path: "src/OpenJustice.BrazilExtractor/Services/Tjgo/TjgoSearchService.cs"
      provides: "TJGO navigation, form fill, submit, and post-submit assertions"
    - path: "src/OpenJustice.BrazilExtractor/Models/TjgoSearchQuery.cs"
      provides: "Search contract with single-day date semantics"
    - path: "src/OpenJustice.BrazilExtractor/Services/Jobs/TjgoSearchJob.cs"
      provides: "Orchestration from worker loop to TJGO service"
  key_links:
    - from: "src/OpenJustice.BrazilExtractor/Worker.cs"
      to: "src/OpenJustice.BrazilExtractor/Services/Jobs/TjgoSearchJob.cs"
      via: "DI-resolved job execution"
      pattern: "GetRequiredService<ITjgoSearchJob>"
    - from: "src/OpenJustice.BrazilExtractor/Services/Jobs/TjgoSearchJob.cs"
      to: "src/OpenJustice.BrazilExtractor/Services/Tjgo/TjgoSearchService.cs"
      via: "ExecuteSearchAsync call"
      pattern: "ExecuteSearchAsync"
    - from: "src/OpenJustice.BrazilExtractor/Services/Tjgo/TjgoSearchService.cs"
      to: "TJGO ConsultaPublicacao form"
      via: "Playwright locators for DataInicial/DataFinal + submit"
      pattern: "#DataInicial|#DataFinal|#formLocalizarBotao"
---

<objective>
Implement the first executable TJGO search vertical slice (browser factory + navigation/form submission + single-day date behavior).

Purpose: Establish the Phase 11 portal automation contract before adding PDF/OCR phases.
Output: End-to-end search job services that use Playwright Chromium and submit same-day query windows.
</objective>

<execution_context>
@/home/eduardo/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/eduardo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-extractor-foundation-and-tjgo-search/11-RESEARCH.md
@.planning/phases/11-extractor-foundation-and-tjgo-search/11-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Playwright Chromium runtime boundary for extractor</name>
  <files>src/OpenJustice.BrazilExtractor/Services/Browser/IPlaywrightBrowserFactory.cs, src/OpenJustice.BrazilExtractor/Services/Browser/PlaywrightBrowserFactory.cs, src/OpenJustice.BrazilExtractor/Program.cs</files>
  <action>Create a dedicated browser factory that initializes Playwright, launches Chromium with deterministic options (headless configurable via options), and returns disposable browser/context handles for job usage. Register this factory in DI and keep browser lifecycle outside `Worker` to avoid duplicated startup code and leaked instances.</action>
  <verify>`dotnet build src/OpenJustice.BrazilExtractor/OpenJustice.BrazilExtractor.csproj` succeeds and worker startup logs indicate Playwright/Chromium initialization path is reachable.</verify>
  <done>Extractor has a reusable, DI-managed Playwright Chromium boundary that can be consumed by TJGO services.</done>
</task>

<task type="auto">
  <name>Task 2: Implement TJGO ConsultaPublicacao navigation and single-day form submission</name>
  <files>src/OpenJustice.BrazilExtractor/Models/TjgoSearchQuery.cs, src/OpenJustice.BrazilExtractor/Models/TjgoSearchResult.cs, src/OpenJustice.BrazilExtractor/Services/Tjgo/ITjgoSearchService.cs, src/OpenJustice.BrazilExtractor/Services/Tjgo/TjgoSearchService.cs, src/OpenJustice.BrazilExtractor/Services/Jobs/TjgoSearchJob.cs</files>
  <action>Implement a TJGO search service that navigates to `ConsultaPublicacao`, fills required fields with locator-first interactions, sets `DataInicial` and `DataFinal` to the same `dd/MM/yyyy` value for single-day runs, submits the form, and asserts result-state contract (URL/state markers). Avoid fixed sleeps; use explicit locator/actionability and URL assertions per research guidance.</action>
  <verify>`dotnet run --project src/OpenJustice.BrazilExtractor/OpenJustice.BrazilExtractor.csproj` executes one search iteration without unhandled exceptions and logs the exact single-day date values submitted.</verify>
  <done>A single-run job can execute the TJGO form workflow end-to-end with same-day date filter semantics preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Wire worker orchestration to execute the TJGO search job</name>
  <files>src/OpenJustice.BrazilExtractor/Worker.cs, src/OpenJustice.BrazilExtractor/Program.cs</files>
  <action>Wire `Worker` to resolve and execute a scoped `ITjgoSearchJob` per run, propagating cancellation tokens and structured logs. Keep orchestration minimal (no portal logic in worker) so phase 12 can extend job behavior without host rewrites.</action>
  <verify>`dotnet run --project src/OpenJustice.BrazilExtractor/OpenJustice.BrazilExtractor.csproj` shows worker start, job execution, and graceful shutdown on cancellation.</verify>
  <done>Worker lifecycle cleanly triggers TJGO job execution through DI-scoped services.</done>
</task>

</tasks>

<verification>
Execute the worker once and confirm Chromium launch, page navigation, same-day date field assignment, and post-submit result-state assertions all happen in one run.
</verification>

<success_criteria>
The extractor can perform a complete TJGO ConsultaPublicacao search flow via Playwright Chromium and enforce single-day date range behavior in submitted queries.
</success_criteria>

<output>
After completion, create `.planning/phases/11-extractor-foundation-and-tjgo-search/11-02-SUMMARY.md`
</output>
